{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main trading window with no filesystem, shell, or provider SDK permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
