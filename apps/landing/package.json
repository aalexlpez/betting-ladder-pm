{
  "name": "@prediction-ladder/landing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run"
  },
  "type": "module",
  "dependencies": {
    "@prediction-ladder/ui": "workspace:*",
    "@vitejs/plugin-react": "6.0.2",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "vite": "8.0.8"
  }
}
