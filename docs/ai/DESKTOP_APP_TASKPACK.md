# Deprecated Alias — Desktop App Taskpack

Use `docs/ai/DESKTOP_APP_DESIGN_TASKPACK.md` instead.

This file remains only to avoid broken references from older notes. Codex should not use it as primary context.
