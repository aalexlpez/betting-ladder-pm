# Context Handoff

Last updated: 2026-06-05.

## Canonical context

This repository version is canonical. Earlier generated packs, Day 1 variants, files under `docs/archive/`, and superseded status reports are non-canonical implementation context unless explicitly requested.

`docs/ai/CONTEXT_INDEX.md` is the canonical route map and source-of-truth map.

## Current timing state

- Total budget: 40 hours.
- Day 1 is closed and logged as 8 hours.
- Day 2 is logged as 8 hours after the product-definition contrast report, repo bootstrap, corrected decisions, and validation pass.
- Day 3 is logged as 8 hours after Goal 02 domain/audit fixes, Goal 03 desktop shell/security cleanup, Goal 04A/04/04B/04C multi-venue market-data work, native tooling verification, and daily traceability reporting.
- Current phase: Goal 07 real-account onboarding is implemented on 2026-06-04 without enabling real trading. The desktop app shows compact Polymarket and Kalshi onboarding status cards that open a modal wizard with official credential URLs and provider-specific "where this comes from" guidance before collecting local references, and Tauri owns secret-free `provider_onboarding_status`, `provider_connect_account`, and `live_preflight_status` commands. Polymarket onboarding accepts a local non-custodial signer file path and rejects seed-like input; the wizard now states that this is not a downloaded Polymarket dashboard API key and must be an approved local signer file prepared outside the renderer. Kalshi onboarding accepts an API Key ID plus a local `.key` private-key file path and validates that the key is parseable unencrypted RSA private-key material; the wizard now states that these come from Kalshi Profile Settings / API Keys / Create New API Key, with the private key saved locally when it is shown. Renderer responses include only masked/status data and blocker reason codes, never private keys, API secrets, passphrases, auth headers, signatures, signed payloads, raw provider payloads, or full wallet/account identifiers. The default credential profile is written under the OS user app-data area through an explicit local secure provider fallback; `.local` and env vars remain dev/smoke fallbacks only. Polymarket account metrics now have a Tauri-owned authenticated provider path: the runtime uses the local signer for CLOB auth, reads USDC balance/allowance plus open orders, reads wallet positions through the provider-owned data API, computes provider/market exposure, and returns only secret-free normalized amounts. Kalshi account metrics now have a Tauri-owned RSA-PSS signed provider path: the runtime uses the local `.key` material only in the main process, signs documented Trade API portfolio requests, reads USD balance, resting orders, and market positions, computes provider/market exposure, and returns only secret-free normalized amounts. Real-live submission still blocks unless provider-owned authenticated account metrics, provider live adapters, legal/geo/local approval, fresh official book, limits, audit, kill switch, explicit acknowledgement, non-marketable policy, and all other Tauri-side gates pass. Provider live adapter readiness still blocks real live with `provider_live_adapter_not_configured` where applicable, especially Kalshi live execution because its live order adapter is not implemented. No real orders were executed.
- Goal 07 UX/security correction, 2026-06-04: provider onboarding no longer presents a permanent manual file path as the normal operator path. The wizard labels the second step as secure import, explains exactly where Polymarket and Kalshi credential inputs come from, and frames path entry as a one-time import source. `provider_connect_account` now reads the source file in Tauri once, validates it, stores app-managed encrypted local credential material under the OS user app-data credential provider, and saves profile metadata pointing to that app-managed store. Polymarket and Kalshi runtime credential loading prefer the encrypted app-managed material and retain legacy local file references only as compatibility/dev-smoke fallback behavior. Renderer state and responses still do not contain signer/key material, API secrets, passphrases, auth headers, signatures, signed payloads, raw provider payloads, or full identifiers. Live remains blocked by all existing legal/geo/account/risk/audit/acknowledgement/non-marketable/provider-adapter gates; connecting credentials does not authorize live execution.
- Goal 07 Traderline-style onboarding follow-up, 2026-06-04: Polymarket onboarding now has a fast Magic/export-assisted path instead of making normal operators create a signer file manually. The modal opens the allowlisted official `https://reveal.magic.link/polymarket` URL in the system browser, tells email/Magic users to copy the revealed signer, and provides an "Import copied key" action. The import action reads the OS clipboard only inside the Tauri main process, validates the signer, writes an encrypted app-managed local credential copy, clears the clipboard after success, and returns only secret-free readiness status to React. Manual local signer file import remains an advanced fallback for external-wallet accounts and dev/smoke compatibility. The localized wizard also names pUSD as the current Polymarket collateral blocker surface: missing pUSD balance, allowance, funder, or deposit-wallet setup must be fixed on Polymarket and does not enable deposits, withdrawals, wrapping, unwrapping, or bypass behavior in the app.
- Goal 07 legal onboarding follow-up, 2026-06-05: the desktop right rail now includes a localized legal approval panel and modal. Normal operators no longer need to hand-edit `.local/legal-gate.local.json`, `LEGAL_GATE_STATUS`, or `ENABLE_LIVE_TRADING` to pass the legal/local approval gate. The modal captures target jurisdiction, real operator identity, responsible approver, first-order stake cap, market exposure cap, and all required C0/C1/no geoblock/VPN/fake-KYC/sanctions bypass/no-custody/audit/no deposit-withdrawal/BUY-only tiny limit-GTC-post-only-non-marketable first-smoke declarations. Tauri owns secret-free `legal_approval_status` and `legal_approval_submit`, validates the declarations, writes the app-managed non-committed local approval file, and lets a valid local approval satisfy the product legal/live-enable source. Env vars remain controlled dev/smoke aliases. Live remains blocked unless credentials, provider-owned account metrics, fresh official book, selected market, stake/exposure, C0/C1, audit, kill switch, explicit acknowledgement, non-marketable policy, and provider adapter gates pass.
- Goal 07 legal onboarding UX follow-up, 2026-06-05: the legal approval modal now presents the full required declaration set as review text plus one explicit acknowledgement checkbox instead of fifteen separate checkbox interactions. React expands that single acknowledgement into the complete boolean declaration payload, and Tauri still validates every required declaration and returns exact `legal_*` blockers when approval is missing. The provider onboarding wizard now opens the legal/local approval flow automatically after a provider credential import succeeds for Polymarket or Kalshi, including the Polymarket Magic clipboard path. The same acknowledgement marks the first-live-smoke session acknowledgement.
- Goal 07 first-live-smoke follow-up, 2026-06-05: React now exposes a narrow secret-free Tauri command bridge for `order_submit_live` and `order_cancel`, and the right rail has a disabled-by-default "submit live smoke and cancel" action. The action is available only in live mode with a validated selected-provider BUY/non-marketable limit/GTC preview, selected-provider preflight ready, fresh official book, app-managed legal/local approval, credential readiness, provider-owned account metrics, audit enabled, kill switch off, and explicit acknowledgement. The submit request goes to Tauri, which reloads provider-owned account metrics and repeats every live gate before using the provider runtime; if a provider order id is returned, React immediately calls Tauri `order_cancel`. Polymarket live runtime is configured by default in Tauri and no longer requires `POLYMARKET_LIVE_RUNTIME_MODE=official_sdk`; `POLYMARKET_LIVE_RUNTIME_MODE=disabled` is a controlled local-off switch. Kalshi live placement remains blocked with `provider_live_adapter_not_configured`. Renderer code still has no private-key/API-secret/auth-header/signature/signed-payload/full-account-id/provider-SDK/direct-provider-network path.
- Goal 07 desktop validation UX follow-up, 2026-06-05: rejected ladder Back/Lay previews now open a localized order-blocked dialog instead of leaving the operator to parse only raw risk reason codes in the right rail. The dialog uses the active `en`/`es`/`ca` desktop catalog, shows the selected side/price/stake when available, keeps secret-free canonical blocker codes as secondary diagnostics, and does not change or bypass deterministic risk validation. The right-rail validation list now also renders localized primary text for core order rejection reasons while preserving the machine code beneath it.
- Goal 07 desktop manual-stake follow-up, 2026-06-05: the ladder stake deck now supports a localized manual stake input alongside the preset buttons. Entering a manual amount clears any stale preview and feeds the same provider-currency `stakeAmount` into the existing deterministic order-intent/risk path. Empty manual stake maps to `stake_not_configured`, invalid decimal input maps to `invalid_stake`, and values above the configured/legal max stake still map to `stake_exceeds_limit`; the change does not raise live limits, bypass account metrics, or add any new Tauri/live submission path.
- Goal 07 desktop funds-diagnostic follow-up, 2026-06-05: blocked ladder/order-preview dialogs now include a localized, secret-free account-metrics diagnostic section showing the provider, selected market when known, account metrics status/source, available funds value read by Tauri, provider exposure, market exposure, and checked-at timestamp. Ladder cell clicks and local preview submits refresh `live_preflight_status` immediately before deterministic validation so stale account-metrics snapshots are less likely to drive confusing insufficient-funds messages. The diagnostic does not expose signer/key material, wallet identifiers, auth headers, signatures, raw provider payloads, or any new live-submit authority, and it does not relax insufficient-funds, marketable-order, legal, geo, credential, audit, kill-switch, acknowledgement, or provider-adapter gates.
- Goal 07 Polymarket signer/funder correction, 2026-06-05: Polymarket onboarding now treats the Magic/exported signer private key and the Polymarket Trading Address / proxy funder as separate required concepts for Magic/proxy accounts. The desktop wizard asks for the public trading/funder address plus signature type, defaults Magic/email users to `proxy`, and explains in English, Spanish, and Catalan that the signer signs while the trading/proxy address is where Polymarket checks pUSD funds, allowance, positions, and open orders. Tauri validates the signature type and funder address before reading the clipboard, stores only app-managed encrypted signer material plus non-secret profile metadata, and returns only masked identifiers/status to React. The Polymarket SDK runtime now authenticates CLOB with `signature_type` and explicit `funder` when configured, uses that same signature type for balance/allowance reads, and uses the trading/funder address rather than the signer address for provider-owned position/exposure metrics. A proxy profile without a trading address is no longer `ready`; it blocks with exact `polymarket_trading_address_*` credential reasons instead of silently reading 0 funds from the wrong address.
- Goal 07 real Polymarket account diagnostic, 2026-06-05: an ignored Rust diagnostic test was added for local operator debugging without exposing secrets or submitting orders. Running it with approved local execution against the then-current imported Polymarket profile decrypted the app-managed signer through DPAPI and performed real read-only CLOB account checks. Result: the imported Magic signer address masked as `0x25...1800`, the configured funder/trading address masked as `0xdf...4876`, the SDK-derived proxy for that signer masked as `0xd8...76C6`, and the SDK-derived safe masked as `0x19...fB44`; read-only CLOB balance checks against both `https://clob-v2.polymarket.com` and `https://clob.polymarket.com` returned `balance=0` for configured proxy, SDK auto proxy, and EOA variants. No private key, API secret, passphrase, auth header, signature, signed payload, full account identifier, raw provider payload, or order submission was printed or executed.
- Goal 07 Polymarket Profile/API address correction, 2026-06-05: official Polymarket guidance for CLOB `Invalid Funder Address` troubleshooting points operators to the Profile address at `https://polymarket.com/settings`, and `POLY_PROXY` is the common Magic Link signature type. The app no longer rejects Magic/Proxy onboarding by locally deriving a proxy address from the imported signer and comparing it to the configured funder, because existing Magic accounts can use the Profile/API address as the public CLOB funder even though Polymarket warns not to send funds to it manually. Tauri still validates signer material, signature type, 0x address shape, required funder presence for non-EOA modes, secret redaction, preflight blocking, and all live gates. The localized wizard now tells operators to copy the Profile/API address from settings and explains that "API-only / do not send funds" means API/CLOB identifier, not a manual deposit destination.
- Goal 07 Polymarket onboarding/cache follow-up, 2026-06-05: the normal Polymarket account-metrics runtime now calls the official CLOB balance/allowance update endpoint before reading collateral balance, and the live Polymarket submit path also refreshes balance/allowance immediately before attempting the provider order. This aligns the product path with Polymarket's current SDK guidance for pUSD/deposit-wallet balance sync instead of leaving refresh behavior only in the ignored diagnostic test. The Magic clipboard import path now accepts a clean 0x signer key or Magic export text/URL containing one; Tauri extracts and validates the signer in the main process, stores only normalized app-managed encrypted signer material, clears the clipboard on success, and still rejects seed phrases. The localized onboarding copy now distinguishes existing Proxy/Magic Profile/API funders from new Deposit Wallet / `POLY_1271` funders, and blocked order diagnostics show available pUSD cash, open-order amount, position exposure, provider exposure, market exposure, source, and checked-at timestamp. A real read-only diagnostic after this change still reported `balance=0` for the then-current local profile: signer masked `0x25...1800`, configured funder masked `0x00...2e49`, SDK proxy masked `0xd8...76C6`, SDK safe masked `0x19...fB44`, configured funder did not match SDK proxy/safe, and both `https://clob-v2.polymarket.com` and `https://clob.polymarket.com` returned `balance=0` after refresh for configured, proxy-auto, and EOA checks. No order was submitted and no secret/full identifier/raw provider payload was printed.
- Goal 07 Polymarket public portfolio diagnostic follow-up, 2026-06-05: Polymarket onboarding/preflight account metrics now include an optional secret-free `publicPortfolioValue` amount sourced from the official public Data API `/value?user=<Profile/API address>` endpoint. This reads only the configured public Profile/API/funder address from the local Tauri-owned profile, never exposes signer/key material or full identifiers to React, and is treated as an optional diagnostic rather than a live gate. The provider onboarding card and credential review step now display separate localized lines for authenticated CLOB trade-ready cash and public positions value, so a user can distinguish "the app sees this public Polymarket profile/positions value" from "the authenticated CLOB pUSD balance/allowance available for new BUY orders is still zero or missing." Rust coverage includes payload parser cases for address matching and empty-value responses. This does not deposit funds, create deposit wallets, approve allowances, bypass Polymarket restrictions, or execute orders.
- Goal 07 Polymarket account-candidate onboarding follow-up, 2026-06-05: provider onboarding status now includes optional secret-free Polymarket account candidates derived inside Tauri from the imported signer and configured profile metadata: configured funder, signer EOA, SDK-derived proxy, and SDK-derived safe, deduplicated when the configured address matches a derived candidate. Each candidate exposes only label, signature type, configured flag, masked address, public portfolio read status, blocker reasons if the public read failed, and optional public positions value; no full account identifier, signer material, auth header, signature, or raw provider payload reaches React. The onboarding card and credential review step now show these candidates in a compact localized list so an operator can see when the configured Profile/API address is not the only plausible Polymarket account address tied to the signer. Candidate public-value reads are diagnostics only; authenticated CLOB pUSD cash/allowance for the configured profile remains the live-buy gate.
- Goal 07 Polymarket candidate-apply onboarding follow-up, 2026-06-05: the normal Magic clipboard import no longer requires the operator to paste a Profile/API funder address before import. For Proxy/Magic with a blank funder, Tauri derives the SDK proxy wallet from the imported signer; for Gnosis Safe with a blank funder, Tauri derives the SDK safe wallet; EOA remains signer-only; `POLY_1271` still requires an explicit deposit-wallet address. The onboarding UI now includes a secret-free "use this account" action for non-configured Polymarket candidates. React sends only the candidate label and signature type; Tauri reloads the local encrypted signer, re-derives the candidate address internally, updates the local secure provider profile, and returns refreshed onboarding/preflight state without exposing the full address, signer, auth material, signatures, or raw provider payloads. This still does not deposit funds, approve allowances, bypass platform restrictions, or execute orders; it only reduces the manual address-selection burden before the existing account-metrics and live gates run.
- Goal 07 Polymarket recommended-candidate onboarding follow-up, 2026-06-05: Polymarket account-candidate diagnostics now mark one secret-free recommended candidate when exactly one non-configured derived account has a public portfolio value greater than the configured account. Tauri computes the recommendation after public Data API value reads, sends only `recommended` plus a non-secret reason code to React, and leaves ambiguous ties or already-positive configured accounts unmarked. The onboarding card and credential review step move the recommended candidate to the top and label it in English, Spanish, and Catalan while preserving the explicit "use this account" click; no account is auto-applied. Public portfolio value remains only an onboarding diagnostic, and authenticated CLOB pUSD cash/allowance plus all legal, geo, credential, risk, audit, acknowledgement, kill-switch, non-marketable, fresh-book, and provider-adapter gates still control live BUY readiness.
- Goal 07 Polymarket local read-only diagnostic after recommended-candidate onboarding, 2026-06-05: running the ignored diagnostic against the current local Polymarket profile printed only masked/secret-free values and submitted no orders. Result: credential status ready, profile signature type `proxy`, signer masked `0x25...1800`, configured funder masked `0x00...2e49`, SDK proxy masked `0xd8...76C6`, SDK safe masked `0x19...fB44`, configured funder did not match SDK proxy/safe, all four candidates (`configured_funder`, `signer_eoa`, `sdk_proxy`, `sdk_safe`) returned public portfolio value `0`, no candidate was recommended, and both CLOB v2 and CLOB v1 returned refreshed collateral balance `0` for configured, proxy-auto, and EOA checks. This suggests the current imported signer/configured profile does not expose trade-ready pUSD or public positions on the known derived accounts; if another tool shows funds, the next investigation should identify which public address and wallet model it is reading without exposing secrets or bypassing provider rules.
- Goal 07 Polymarket Magic-export address capture follow-up, 2026-06-05: the Magic clipboard import now keeps non-secret public `0x` addresses found in the copied Magic text/URL as app-managed local profile metadata, while still extracting and encrypting only the signer material in Tauri and clearing the clipboard after success. Public address extraction rejects zero addresses, deduplicates/caps candidates, and does not treat the first 40 hex characters of a 64-hex private key as an address. Account-candidate diagnostics now include those imported public addresses as `POLY_1271` deposit-wallet candidates unless they match the signer EOA or SDK-derived proxy/safe, with stable suffixed labels when multiple imported addresses exist so React can apply a candidate by label/signature type without sending a full address. Existing profiles remain compatible through a default empty candidate list, but operators must reimport Magic text/URL for previously discarded public addresses to become candidates. No account is auto-applied; no deposits, withdrawals, approvals, provider restrictions bypass, live-limit changes, or live orders were added.
- Goal 07 Polymarket candidate cash diagnostic closure, 2026-06-05: Polymarket account candidates now include a Tauri-owned authenticated CLOB `tradeReadyCash` diagnostic in addition to the public portfolio value. Tauri authenticates each candidate with the local signer plus the candidate's signature type/funder, refreshes balance/allowance, returns only secret-free amounts/status/reason codes, and prioritizes a positive trade-ready cash signal when recommending a candidate. The onboarding candidate list now shows trade-ready cash per masked candidate, making it clear whether "sin dinero" means the configured account truly has no pUSD/allowance or the funds sit on another detected candidate. No account is auto-applied and no deposits, withdrawals, approvals, geoblock bypass, live-limit change, or live order path was added.
- Goal 07 Polymarket/legal gate stabilization follow-up, 2026-06-05: the Polymarket fast-connect modal no longer reimports from the clipboard when the local Polymarket profile is already `ready` and no advanced signer file path is being supplied; it advances directly to the legal approval flow instead of allowing a cleared clipboard to overwrite the visible wizard state with a transient invalid import result. The Tauri main capability now references an app-owned `app-trading-commands` permission containing the narrow registered command boundary, including `legal_approval_submit`, without adding filesystem, shell, dialog, HTTP, process, or websocket grants. `apps/desktop/src-tauri/build.rs` now marks the capability and app permission files as Cargo rebuild inputs so permission changes are embedded in the next dev binary. A read-only diagnostic after this follow-up still reported the local Polymarket profile as `ready`, `poly_1271`, configured funder masked `0x00...2e49`, and authenticated CLOB trade-ready cash `5 pUSD`; no live order was submitted.
- Goal 07 Polymarket legal-provider selection follow-up, 2026-06-05: the legal approval panel/modal now prefers Polymarket as the active legal provider whenever the Polymarket credential is `ready`, instead of deriving the legal target from the currently selected market/provider. React also filters cached legal approval status by provider before rendering, so a stale Kalshi `provider-error` cannot appear inside the Polymarket legal flow. The regression test locks this wiring and blocks the old `providerId={props.state.selectedProviderId}` rail path. Validation passed with `pnpm --filter @prediction-ladder/desktop typecheck`, `pnpm --filter @prediction-ladder/desktop test` (61 tests), `C:\Users\1\.cargo\bin\cargo.exe test legal_approval -- --nocapture`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, and `git diff --check`. A fresh read-only Polymarket diagnostic still reported credential `ready`, profile `poly_1271`, configured funder masked `0x00...2e49`, and authenticated CLOB trade-ready cash `5 pUSD`; no live order was submitted.
- Goal 07 live-mode blocker clarity follow-up, 2026-06-05: the desktop execution-mode strip no longer labels every non-submit-ready `LIVE` state as generic `blocked by gates`. `LIVE` now separates base legal/credential/account/ack blockers from market setup blockers (`selected_market_missing`, `fresh_official_order_book_missing`) and order-specific blockers (`marketable_order_blocked`, `live_smoke_buy_only`, stake/exposure/quantity policy). The mode tile displays the first exact blocker when one exists, while the live smoke submit button remains blocked until a validated BUY limit/GTC/post-only/non-marketable preview, selected fresh official market, Polymarket preflight, legal/local approval, credential/account metrics, audit, kill-switch, acknowledgement, and Tauri/provider adapter checks all pass. Validation passed with `pnpm --filter @prediction-ladder/desktop typecheck`, `pnpm --filter @prediction-ladder/desktop test` (62 tests), `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, and `git diff --check`; no live order was submitted.
- Goal 07 Polymarket provider-rejection clarity follow-up, 2026-06-05: after a real operator attempt returned only `provider_rejected`, React now renders the secret-free provider response `message` in the live smoke result panel instead of hiding it behind the canonical reason code. The live smoke submit path also refreshes the selected official order book immediately before sending, rebuilds the BUY/SELL preview against that fresh book, updates validation diagnostics, and blocks locally if the order became marketable/stale/invalid before reaching Tauri. This keeps post-only Polymarket orders from relying on a stale preview while still preserving all legal, credential, account, audit, acknowledgement, kill-switch, non-marketable, and provider-adapter gates. Validation passed with `pnpm --filter @prediction-ladder/desktop typecheck`, `pnpm --filter @prediction-ladder/desktop test` (62 tests), `pnpm --filter @prediction-ladder/desktop lint`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, and `git diff --check`. No live order was submitted by Codex.
- Goal 07 Polymarket live-smoke stake/quantity blocker correction, 2026-06-05: the Tauri `order_submit_live` validation no longer reports the misleading `quantity_exceeds_stake_amount` blocker. Polymarket BUY quantity is shares/contracts, so a `5 pUSD` stake at `0.5` correctly implies `10` shares. The live submit gate now treats the blocker as `estimated_order_cost_exceeds_stake_amount`, compares `price * quantity` against declared stake with a small decimal tolerance, and includes the secret-free estimated cost/stake calculation in the blocked response message. React classifies the new reason as an order-specific blocker. Rust regression coverage proves the normal `5 @ 0.5 => 10 shares` case is allowed, a huge quantity is blocked by estimated cost, and repeating-decimal rounding at the declared stake is tolerated. The legal-gate Rust test was also isolated from real local Polymarket credentials so the suite does not depend on the operator's saved profile. No live order was submitted by Codex.
- Goal 07 Polymarket live-smoke provider-rejection diagnostic follow-up, 2026-06-05: a real operator retry reached Polymarket and came back as `provider_rejected` with the generic message "Polymarket rejected the live provider request." The root cause was not another local gate: the SDK returned a provider/status error and Tauri's `polymarket_error_message` collapsed all non-geoblock/non-credential status errors into that generic text. Tauri now extracts the SDK status/validation body, redacts long `0x` hex identifiers, truncates the message, and maps known official Polymarket rejection classes to operator-readable diagnostics: post-only crossing the book, market minimum size, tick-size/price increment, balance/allowance, duplicate order, and market-not-ready. The Polymarket order-book snapshot now transports optional `minOrderSize` from `min_order_size`, React includes it in `order_submit_live`, and Tauri blocks `order_size_below_market_minimum` before provider submission when size is below the market minimum. This specifically covers the observed `stake=1`, `price=0.49`, `quantity=2.040816326530612244` style attempt if the market minimum is `5`. No live order was submitted by Codex.
- Goal 07 Polymarket empty-provider-body diagnostic follow-up, 2026-06-05: a later operator attempt still showed the generic "Polymarket rejected the live provider request." The remaining gap was that the SDK `Status` error can carry an empty provider body, and the Tauri mapper only inspected `status.message`; when that was empty it dropped back to the generic text. `map_polymarket_order_error` now receives the secret-free live order request context and `polymarket_error_message` falls back to HTTP status/method/path plus context (`side`, `price`, `size`, `stake`, `postOnly`, freshness, and optional market minimum size) instead of ever returning the opaque generic string for provider/status rejects. No signer, key material, auth headers, signatures, signed payloads, raw provider payloads, or full wallet identifiers are logged or returned. No live order was submitted by Codex.
- Goal 07 Polymarket official-CLOB host correction, 2026-06-05: the surfaced provider rejection `HTTP 405 Method Not Allowed while calling POST /order` showed that the app was submitting through the wrong default CLOB host for trading. Official Polymarket API docs list CLOB trading operations, including `POST /order`, under `https://clob.polymarket.com`, while the Tauri live and account-metrics runtimes had defaulted `POLYMARKET_CLOB_API_URL` to `https://clob-v2.polymarket.com`. The default runtime host is now the official CLOB host for live place/cancel and account-metrics calls unless a local developer override explicitly sets `POLYMARKET_CLOB_API_URL`; order-book reads may still probe both CLOB hosts for compatibility. This does not change credentials, legal gates, risk limits, non-marketable smoke policy, or provider-submission boundaries. No live order was submitted by Codex.
- Goal 07 open-live-order follow-up, 2026-06-05: after the operator confirmed a real Polymarket live order could be submitted and cancelled successfully, the desktop right rail no longer auto-cancels successful live submissions. The primary live action is now localized as "Create live order" / "Crear orden live" and still requires live mode, a validated BUY/non-marketable limit/GTC preview, selected-provider preflight ready, fresh official book, app-managed legal/local approval, credential readiness, provider-owned account metrics, audit enabled, kill switch off, and explicit acknowledgement. React calls only Tauri `order_submit_live`; if Tauri returns a provider order id, the renderer records a local session open-order row and exposes cancellation as a separate manual Tauri `order_cancel` action by id. The legal acknowledgement copy now describes the live-order policy with manual cancellation available instead of promising immediate cancellation. Renderer code still has no private-key/API-secret/auth-header/signature/signed-payload/full-account-id/provider-SDK/direct-provider-network path. No live order was submitted by Codex.
- Goal 07 desktop UI cleanup, 2026-06-05: the current desktop renderer now presents a Polymarket-only visible shell without deleting the underlying multi-venue base. The left market rail no longer shows redundant venue connection cards or Kalshi search/filter surfaces; it starts in a Polymarket-only browse/search path, shows compact Polymarket summary chips plus selected-market focus, and uses denser market cards with clearer spacing and outcome selection. The right rail now shows only the Polymarket onboarding/status card and provider-scoped financial metrics; the Kalshi credential card, the "Resumen de puertas live" / live gate summary section, and the audit-log section are hidden from the active frontend. Tauri commands, provider adapters, live gates, and secret-handling boundaries were not relaxed or bypassed by this cleanup.
- Day 4 traceability reporting is complete on 2026-06-04: `docs/status/DAILY_REPORT_2026-06-04.md` summarizes the day in Spanish for evaluators, and `docs/status/TIME_LOG.md` now records Day 4 as an 8-hour block. This is status evidence only; it does not change the next action.
- Final report validation exposed and fixed a small desktop onboarding wizard wiring issue: `CredentialGuideStep` now receives localized `providerOnboarding` messages and reads provider guides from the catalog instead of the removed static guide constant. `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, and `git diff --check` pass after the fix.
- Latest validation after the Polymarket public portfolio diagnostic follow-up: `pnpm --filter @prediction-ladder/desktop test`, `corepack pnpm --filter @prediction-ladder/i18n test`, `C:\Users\1\.cargo\bin\cargo.exe test`, `pnpm typecheck`, `pnpm lint`, `corepack pnpm test` with approved execution, `corepack pnpm build` with approved execution, and `git diff --check` pass. Plain `pnpm test`/`pnpm build` failed only because `pnpm` was not visible in that sandbox shell, and `corepack pnpm test`/`corepack pnpm build` failed inside the sandbox because Turbo could not resolve the package-manager binary; the approved out-of-sandbox reruns passed.
- Latest validation after the Polymarket account-candidate onboarding follow-up: `pnpm --filter @prediction-ladder/desktop test`, `corepack pnpm --filter @prediction-ladder/i18n test`, `C:\Users\1\.cargo\bin\cargo.exe test`, `pnpm typecheck`, `pnpm lint`, `corepack pnpm test` with approved execution, `corepack pnpm build` with approved execution, and `git diff --check` pass. The desktop dev server responded HTTP 200 at `http://127.0.0.1:1420/`.
- Latest validation after the Polymarket candidate-apply onboarding follow-up: `pnpm --filter @prediction-ladder/desktop test`, `corepack pnpm --filter @prediction-ladder/i18n test`, `C:\Users\1\.cargo\bin\cargo.exe test`, `pnpm typecheck`, `pnpm lint`, `corepack pnpm test` with approved execution, `corepack pnpm build` with approved execution, and `git diff --check` pass. The desktop dev server responded HTTP 200 at `http://127.0.0.1:1420/`.
- Delivery target: launched or launch-ready first version by the end of the 40-hour budget.

## Current status

Goal 01 repo bootstrap is complete. The repository now has a working pnpm/Turborepo workspace with minimal Tauri/Vite/React desktop and Vite/React landing entrypoints, real workspace scripts, strict TypeScript, ESLint, Vitest smoke tests, and a generated `pnpm-lock.yaml`.

Goal 01 audit result on 2026-06-02: **CONDITIONAL_PASS**. No critical blocker was found before Goal 02, and Goal 02 is now complete.

Goal 02 domain core with tests is complete after audit-blocker fixes on 2026-06-03. The fix added `decimal.js` as a real `@prediction-ladder/core` dependency, replaced the temporary bigint decimal engine with `decimal.js`-backed helpers, added deterministic economic-value rejection paths, added the live/live-dry-run audit log safety gate, and expanded the core suite to 44 deterministic tests.

Goal 03 Tauri desktop shell with provider-ready states is complete on 2026-06-03. The desktop renderer now uses a first-pass Windows trading terminal shell instead of the bootstrap panel: top command/status strip, left provider/market rail, central ladder workspace, right risk/order rail, and bottom audit/status strip. Polymarket and Kalshi provider surfaces are present without external API calls. Market search, ladder, metrics, open orders, audit, and order preview surfaces are honest placeholders. Execution mode defaults to disabled, one-click is off and cannot arm, live is visibly blocked, the kill switch is visible, and legal/geo/credential/audit/live gate summaries stay on screen. The Tauri command/capability boundary remains unchanged and narrow: no renderer filesystem, shell/process, provider SDK, credential, secret, or live execution access was added.

Goal 03 follow-up debt cleanup is complete on 2026-06-03. The desktop shell now has explicit Tauri/security regression coverage for `withGlobalTauri: false`, self-only CSP with no remote HTTP/WebSocket endpoints, the main-window-only `core:default` capability set, and absence of filesystem/shell/dialog/process/http grants. The main Tauri minimum width is aligned to the desktop terminal spec at 1280px, the CSS fallback minimum was aligned with it, and remaining visible desktop placeholders for connection/query/account-metrics states were moved into `@prediction-ladder/i18n` catalogs. No provider APIs, credentials, live adapters, or order submission path were added.

Goal 04A normalized multi-venue contracts is complete on 2026-06-03. `packages/core` now owns provider-neutral `TradableMarketRef`, outcome, order-book, freshness, connection, balance, position, fee, order, fill, and settlement contracts plus deterministic order-book normalization helpers. `packages/market-data` now exposes contract-only discovery/order-book adapter ports, explicit adapter errors, source metadata, and fixture-vs-live origin distinction. `packages/execution` now exposes contract-only execution and account-state ports with explicit blocked/error states. `packages/market-data` and `packages/execution` depend only on `@prediction-ladder/core` and report `contracts_ready`; no external API calls, credentials, provider SDKs, live execution, or fixture-as-live behavior was added. The desktop provider switch now consumes canonical `PROVIDER_IDS` from `@prediction-ladder/core`.

Goal 04A debt cleanup is complete on 2026-06-03. `MarketResolveRequest` no longer inherits from normalized `MarketRef` or requires `currency` before adapter resolution, `isTradableMarketRef` now uses the full `createTradableMarketRef` validation path, i18n catalogs include the `contracts_ready` implementation status, and `docs/START_HERE_FOR_CODEX.md` lists Goal 04A in the reusable goal catalog.

Goal 04 real read-only provider data is complete on 2026-06-03. `packages/market-data` now reports `read_only_adapters_ready` and includes injectable official REST adapters for Polymarket and Kalshi discovery, market resolution, and order-book snapshots. Polymarket uses public Gamma/CLOB read paths and Kalshi uses the documented public markets/orderbook paths while treating any `401`/`403` response as `provider_credentials_required` rather than success. Kalshi YES/NO bids are normalized into selected-side bids and implied asks without leaking provider shape outside adapter code or `providerMetadata`. Fixture and configured fallback sources remain distinct from `official_live`; stale data, unavailable providers, malformed payloads, unsupported markets, provider errors, missing outcomes, invalid ticks, credential-required responses, and empty liquidity return explicit adapter errors. The desktop shell can consume normalized read-only snapshots and render shared ladder rows, but renderer-side provider calls, order submission, one-click arming, account metrics, and live execution remain blocked.

Goal 04 audit result on 2026-06-03: **CONDITIONAL_PASS** for REST snapshot/read-only adapter work. Audit fixes closed a Kalshi status-normalization bug, removed live-like freshness/connection metadata from configured fallback requests, aligned market-search `connectionMode` with polling fallback behavior, and corrected stale documentation that implied WebSocket runtime belonged in Goal 04. At audit time the remaining non-blocking debt was scoped to Goal 04B/04C; Goal 04B and Goal 04C are now implemented. Detailed audit notes are in `docs/status/GOAL_04_AUDIT_2026-06-03.md`.

Goal 04B and Goal 04C were added on 2026-06-03 after the product requirement was clarified: the final app must use official provider SDKs where available or official provider OpenAPI/AsyncAPI/direct documented clients where no official SDK exists for the selected runtime, and the fresh market-data path should be WebSocket-first rather than REST-polling-first. Polymarket official docs currently list TypeScript, Python, and Rust CLOB clients plus an unauthenticated market WebSocket channel. Kalshi official docs currently list Python and TypeScript SDKs, recommend OpenAPI/AsyncAPI specs as the source of truth for active production clients, and require authentication during WebSocket connection setup even for public market-data channels. The desktop market experience must become unified across venues: one normalized ladder/search surface with compact provider badges/icons, not a provider-switch-first product mode. Provider identity still remains visible when it affects legal, currency, credential, fee, account, order, or execution safety.

Goal 04B official provider runtime and WebSocket-first streaming is implemented on 2026-06-03. Official provider docs were revalidated immediately before implementation: Polymarket documents official TypeScript/Python/Rust CLOB clients and a direct public market WebSocket channel; Kalshi documents Python/TypeScript SDKs, recommends OpenAPI/AsyncAPI as production source-of-truth, and requires authenticated WebSocket handshakes. `packages/market-data` now reports `official_runtime_streaming_ready`, documents the provider runtime choices, normalizes Polymarket WebSocket `book`/`price_change` and Kalshi WebSocket `orderbook_snapshot`/`orderbook_delta` messages into shared order-book snapshots, models connecting/connected/reconnecting/stale/disconnected/invalid/blocked/credentials-required/unavailable/provider-error states, handles out-of-order updates, stale timeouts, malformed messages, auth-required Kalshi handshakes, and REST recovery/fallback, and projects only normalized secret-free market data to the renderer. `apps/desktop/src-tauri` now registers secret-free `market_search`, `market_get_order_book`, and `market_subscribe` command boundaries. No live order placement, cancellation, account balances, positions, provider credentials, auth headers, signing payloads, geoblock workaround, bots, strategies, copy trading, or AI signals were added.

Goal 04B audit result on 2026-06-03: **CONDITIONAL_PASS** after fixing provider-specific error projection and stale audit/spec wording. Kalshi stream/runtime errors now remain attached to Kalshi instead of defaulting to Polymarket in renderer-facing state, and the desktop model keeps provider-specific error cards aligned. Detailed audit notes are in `docs/status/GOAL_04B_AUDIT_2026-06-03.md`.

Goal 04C unified multi-venue desktop market experience is implemented on 2026-06-03 and received a post-audit UX/data-path correction the same day. The desktop renderer now has a unified market search workflow wired to the Tauri market-data command boundary: the app automatically requests the first bounded page of all supported markets on startup, an empty search box means browse all venues, typed search still queries `market_search`, results from Polymarket/Kalshi appear in one list with compact venue badges, selecting an outcome calls `market_get_order_book`, the central ladder renders normalized bid/ask rows when a real official snapshot is available, and selection also calls `market_subscribe` so the returned connection state is visible in the ladder header and bottom strip. The Rust/Tauri command handlers are no longer static stubs: Polymarket discovery, market resolution, and order-book bootstrap use the official Rust SDK (`gamma` and `clob` features) and remain secret-free; Kalshi discovery/orderbook uses the documented Trade API REST path with the recommended `external-api.kalshi.com` host first and the documented `api.elections.kalshi.com` host as fallback, mapping `401`/`403` to `credentials-required`; Kalshi WebSocket remains `credentials-required`; Polymarket WebSocket returns explicit `unavailable` with SDK/REST snapshot fallback documented because no safe persistent Tauri WebSocket transport/session is configured in this command path. No live order placement, cancellation, balances, positions, provider credentials, auth headers, signing payloads, geoblock workaround, bots, strategies, copy trading, or AI signals were added.

Goal 04C large audit correction on 2026-06-03 addressed runtime symptoms reported from the desktop app: Kalshi fallback URL composition now strips the Trade API prefix before switching hosts, preventing `https://api.elections.kalshi.com/trade-api/v2/trade-api/v2/markets...`; unified market search now carries secret-free pagination state with Polymarket Gamma offsets and Kalshi documented cursors; Kalshi typed queries remain bounded because the documented markets endpoint has cursor/ticker/event/series/status filters rather than a public free-text search parameter, so exact-looking `KX...`/hyphenated identifiers use `tickers` first and general text scans only a small number of cursor pages; partial provider success is now explicit in the aggregate message, including connected venue counts and blocked venue states; the left rail now renders compact all/Polymarket/Kalshi filter counts plus a per-venue diagnostic strip beside the search summary with provider status, connection mode, freshness, visible result count, page state, and latest message/error; the left rail has direct all/Polymarket/Kalshi filters plus load-more/infinite-scroll behavior; compact venue badges include a small visual mark; and the visible `WebSocket` label was changed to `Stream` so a blocked/unavailable persistent stream is not confused with failed snapshot observability. Snapshot/order-book data and stream state remain separate: Kalshi stream is still credentials-required and Polymarket stream is still unavailable in this command path until a safe persistent Tauri WebSocket session exists.

Goal 04C Kalshi network diagnosis on 2026-06-03: official docs still list `https://external-api.kalshi.com/trade-api/v2` as the recommended production REST base and `https://api.elections.kalshi.com/trade-api/v2` as supported fallback. On this machine, system DNS resolves both hosts to `192.187.20.203`, while public DNS resolves `external-api.kalshi.com` to the AWS ELB `elections-external-api-107430227.us-east-2.elb.amazonaws.com` and `api.elections.kalshi.com` to CloudFront IPs. `curl.exe` with normal TLS verification fails with `SEC_E_WRONG_PRINCIPAL`; `curl.exe -k` reaches an Apache/Red Hat `403`, confirming this is a local DNS/proxy/network-filtering problem rather than the earlier duplicated-path bug. The Tauri runtime now surfaces Kalshi TLS/certificate failures as explicit `disconnected` / `network_error` diagnostics and must not disable TLS, pin transient IPs, or route around local network policy.

Goal 04C ladder-first desktop UI correction on 2026-06-04: the desktop app was adjusted after manual product review clarified that the core task is a Betfair-style peer-to-peer betting ladder, not a Traderline/card-board clone. The market rail remains the unified Polymarket/Kalshi discovery and infinite-scroll access path, with compact venue diagnostics and no duplicated provider-health section. The center workspace is now the primary execution surface: Back / price / Lay ladder columns, disabled direct price interaction targets, stake presets next to the ladder, visible one-click state, keyboard/order-intent placeholders, stale/disconnected overlay, and best-price styling hooks. The right rail was reduced toward risk, kill switch, order preview, metrics, gates, open orders, and audit; stake presets and one-click are no longer duplicated there. These controls are visual/blocked affordances only: no order intent, order submission, cancellation, live execution, credentials, bots, or automated strategies were added.

Goal 04C local verification: start or restart the Tauri desktop dev shell. In a Tauri desktop window, the left rail should auto-load a bounded first page of provider-backed Polymarket/Kalshi markets without requiring a query. Pressing search with an empty box should reload that all-venues page; typing a query should filter through `market_search`; the all/Polymarket/Kalshi segmented filter should request provider-scoped results; scrolling to the bottom or pressing Load more should request the next bounded page without clearing the selected ladder. The search summary should be followed by per-venue diagnostics for Polymarket and Kalshi so partial blockers, credentials-required states, TLS/DNS failures, freshness, connection mode, visible result counts, and page availability are visible next to the market list. Selecting an outcome should load a normalized ladder snapshot through `market_get_order_book` when the provider path is available, then show the `market_subscribe` state as Stream status. In a plain browser tab without the Tauri command bridge, the same UI remains visible but returns explicit `unavailable` states and does not call provider APIs directly from React.

Goal 04C audit result on 2026-06-03: **CONDITIONAL_PASS**, followed by an implementation correction after manual desktop smoke showed that the app appeared static and empty-search behavior conflicted with the intended all-markets browse state. The correction changed empty search to a real all-venues browse request, added startup market loading in the renderer, moved Polymarket command handling to the official Rust SDK, added Kalshi documented-host fallback, and expanded desktop tests for the new browse contract. Earlier audit fixes also separated selected venue snapshot/data connection from stream/WebSocket availability, added a 12-second timeout to Tauri-side read-only provider HTTP requests, and kept renderer CSP at `connect-src 'self'` while provider networking stays Rust/Tauri-owned. Native `cargo check` passed for `apps/desktop/src-tauri` after the SDK addition; `pnpm typecheck`, `pnpm lint`, `pnpm test`, and `pnpm build` passed with `corepack pnpm` on 2026-06-03.

Goal 05 paper/live-dry-run order intent and local audit log is implemented on 2026-06-04. Back/Lay ladder cells now create provider-neutral `OrderIntent` previews from selected fresh official order books, domain validation covers selected market, data freshness, tick alignment, configured stake, stake/exposure caps, kill switch, one-click state, execution mode, account metric requirements, and legal/geo/credential/live gates, and the execution package routes disabled, paper, live-dry-run, and live-blocked modes through deterministic local contracts. Paper confirmation creates local paper order records only; live-dry-run records a local dry-run check and never submits externally; live/live-blocked remains blocked because real provider submission is not implemented in this goal. The desktop right rail now shows real intent preview, validation reasons, local paper orders, and redacted audit events instead of pure placeholders. Tauri gained no order-submission command, no credential path, and no provider SDK/secret access from the renderer.

Goal 05 audit correction on 2026-06-04 fixed two critical desktop inconsistencies found during manual review. First, visible UI/i18n copy no longer says keyboard/order clicks are "pending Goal 05": it now states that Back/Lay clicks create safe order-intent previews, while keyboard order shortcuts are still not wired. Paper and live-dry-run mode summaries now describe implemented local harness behavior instead of future work. Second, stale accepted previews are cleared or converted to rejected state when stake/mode changes, an executable order book disappears, or an invalid preview/submit path is attempted, so a previous approved intent cannot leave the local confirm button enabled. Added regression coverage for these cases and for the absence of Tauri order submission/cancellation commands before the live gate slice.

Goal 06 partial implementation on 2026-06-04 added the first gated live-ready execution vertical slice without enabling real trading. `packages/execution` now exposes secret-safe `CredentialProvider`, non-committed `LocalApprovalGateProvider`, provider-neutral live place/cancel contracts, provider order id handling, provider rejection/network-error mapping, and `GatedLiveExecutionAdapter` coverage with mocked live place success, mocked live cancel success, missing credentials, missing local approval, domain gate failure, provider rejection, network error, kill-switch, and audit-redaction tests. `apps/desktop/src-tauri` now registers `live_gate_status`, `order_submit_live`, and `order_cancel` as narrow secret-free commands. `order_submit_live` repeats runtime gates for provider support, `ENABLE_LIVE_TRADING`, `LEGAL_GATE_STATUS`, `.local/legal-gate.local.json` or `LEGAL_APPROVAL_FILE`, local credential-source readiness, privileged account-metrics-source readiness, explicit acknowledgement, audit logging, kill switch, selected market, fresh official order book, BUY/limit/GTC/non-marketable smoke policy, stake, funds, provider exposure, and market exposure before any provider runtime branch. The Tauri command layer now has a `LiveProviderRuntime` seam for place-limit and cancel-by-provider-order-id behavior; Rust tests inject a mocked Polymarket runtime to prove no provider call occurs while a live submit gate is blocked and to cover mocked submit success, cancel success, provider rejection, and network error. Historical note: in this slice the default runtime returned `provider_live_adapter_not_configured` and the Polymarket official SDK branch required `POLYMARKET_LIVE_RUNTIME_MODE=official_sdk`; the 2026-06-05 first-live-smoke follow-up above supersedes that by configuring Polymarket by default while keeping all Tauri gates. Kalshi live remains blocked pending an authenticated live adapter. The desktop model/UI now displays local approval, explicit live acknowledgement, account metrics source, account metrics, and provider-adapter readiness as exact live blockers; renderer state still contains no private keys, seed phrases, API secrets, auth headers, signatures, signed payloads, provider SDK objects, raw provider payloads, or full wallet/account identifiers.

Goal 06 audit correction on 2026-06-04 fixed critical live-boundary issues found during review. Tauri no longer trusts renderer-supplied `quantity` without checking `price * quantity <= stakeAmount`, no longer accepts projected exposure based only on renderer-provided market exposure, and now enforces non-committed local approval max stake/exposure caps with Decimal validation. `order_submit_live` also now requires fresh Tauri-owned account metrics values loaded from a non-committed local account metrics file, defaulting to `.local/account-metrics.local.json` or `LOCAL_ACCOUNT_METRICS_FILE`; the file must match provider and market and pass freshness checks before funds/exposure can approve live submit. The renderer consumed only the secret-free `live_gate_status` command in that slice and threaded Tauri-owned blocker reasons into the visible gate panel. Added adversarial Rust and desktop tests for renderer quantity inflation, projected exposure, local approval caps, missing Tauri-owned account metrics values, account metrics provider/market/freshness checks, secret-free live gate status, no renderer order command client in that slice, and Tauri-authoritative gate blockers.

Goal 07 real-account onboarding implementation on 2026-06-04 added a desktop account onboarding panel and Tauri-owned provider credential/preflight commands without enabling real trading. `provider_onboarding_status` and `live_preflight_status` return exact per-provider ready/blocked reasons and account metric readiness without secrets. `provider_connect_account` accepts only non-secret local references: a Polymarket local signer file path or Kalshi API Key ID plus local `.key` private-key file path. Tauri validates those references, rejects seed-like material, rejects encrypted/passphrase-dependent Kalshi keys, requires Kalshi `.key` material to parse as unencrypted RSA private-key material, and stores profile metadata through the explicit local secure provider profile under OS app data by default; `.local`/env fallback paths are dev/smoke only. React never receives raw key material, auth headers, signatures, signed payloads, passphrases, provider SDK objects, raw provider payloads, or full wallet/account identifiers. Polymarket account metrics use a Tauri-owned authenticated provider runtime before the dev/smoke local JSON fallback: CLOB balance/allowance, CLOB open orders, and provider data API positions are normalized into secret-free funds, open-order amount, provider exposure, and market exposure. Kalshi account metrics use a Tauri-owned RSA-PSS signed portfolio runtime before the dev/smoke local JSON fallback: the main process signs documented Trade API portfolio requests with `KALSHI-ACCESS-*` headers, strips query parameters from the signed path, reads USD balance, resting orders, and market positions, and normalizes them into secret-free funds, open-order amount, provider exposure, and market exposure. Rust coverage now includes secret-free onboarding/preflight responses, Polymarket seed rejection, Kalshi parseable-RSA key validation, signature path construction, profile persistence without returning secret paths/key material, provider-owned account metrics readiness without a local metrics file for both providers, Kalshi metrics normalization, mocked Kalshi submit/cancel success after all gates pass, and the existing Polymarket mocked place/cancel path. Provider live adapter readiness and all legal/geo/local approval/risk/audit/acknowledgement/non-marketable gates still block real live; Kalshi live order placement still reports `provider_live_adapter_not_configured`.

Day 3 traceability reporting is complete on 2026-06-03: `docs/status/DAILY_REPORT_2026-06-03.md` summarizes the day in Spanish, and `docs/status/TIME_LOG.md` now records Day 3 as an 8-hour block. This is status evidence only; it does not change the next implementation block.

Post-Goal-02 canonical state correction on 2026-06-03 resolved stale Goal 02 next-action text in `AGENTS.md`, `README.md`, `docs/START_HERE_FOR_CODEX.md`, and `docs/status/TIME_LOG.md`; marked the Day 1 daily report as historical/superseded for implementation decisions; replaced misleading package `ready: true` status objects with `boundaryReady` plus `implementationStatus`; and kept active `gpt-5.5`/screen-recording references intentional rather than claiming they were absent.

Documentation authority reduction on 2026-06-03 made this handoff the only detailed current-state and next-action source, moved the authority model into `docs/ai/CONTEXT_INDEX.md`, shortened entry docs into pointers instead of competing roadmaps, and marked support/status/launch traceability docs so they cannot override the current handoff.

Git/GitHub preparation on 2026-06-03 initialized the local repository plan for the empty `aalexlpez/betting-ladder-pm` GitHub remote, strengthened ignore/line-ending/editor conventions, and kept local/generated/sensitive files out of the first commit.

Remaining environment caveats:

- In the Codex sandbox, plain `pnpm` is not visible without approval because the installed shim lives under the user-level npm bin path. The required pnpm validations pass when the user-level pnpm command is allowed to run.
- Local Node.js was upgraded to `22.22.3` on 2026-06-03, satisfying the root engine range and Vite's preferred `20.19+` or `22.12+` floor.
- In the Codex sandbox, `cargo` is available through `C:\Users\1\.cargo\bin\cargo.exe` but not as plain `cargo` on PATH. Native `cargo check` passes for `apps/desktop/src-tauri`; Windows installer packaging remains deferred to the installer/distribution goal.
- Git commands in the Codex sandbox required adding this workspace to Git `safe.directory` because the sandbox user differs from the Windows workspace owner.

Latest bootstrap changes applied:

- installed `pnpm@10.0.0` and generated `pnpm-lock.yaml`;
- added root engine metadata for Vite's supported Node range: `>=20.19.0 <21 || >=22.12.0`;
- added `packages/i18n` as a dependency-free typed locale catalog package for English, Spanish, and Catalan UI copy;
- added minimal source entrypoints and Vitest smoke tests for `packages/core`, `packages/ui`, `packages/market-data`, `packages/execution`, and `packages/config`;
- replaced the `apps/desktop` disabled-mode bootstrap screen with a provider-ready terminal shell and deterministic fake-free desktop state model, still with no provider/live execution logic;
- added `apps/desktop/src-tauri` Tauri v2 shell scaffold with one secret-safe `app_get_status` command and only `core:default` capability permissions;
- added `apps/landing` Vite React static entrypoint with explicit non-trading bootstrap copy;
- renamed ESLint flat config to `eslint.config.mjs` so workspace lint commands load it correctly;
- expanded ESLint global ignores so nested generated artifacts such as `apps/desktop/dist` and `apps/landing/dist` are not linted after builds;
- pinned `@rolldown/binding-win32-x64-msvc@1.0.0-rc.15` because the Windows Vite/Vitest path required the native Rolldown binding;
- allowed only `esbuild` as an approved pnpm built dependency;
- started Vite dev servers for browser smoke verification at `http://127.0.0.1:1420/` and `http://127.0.0.1:5174/`.

Latest critical documentation fixes applied:

- updated `docs/status/TIME_LOG.md` to log Day 1 and Day 2 as 8-hour daily blocks with traceability to the Day 1 consolidated report and Day 2 contrast report; running total is now 16h of 40h;
- rewrote `docs/status/PRODUCT_DEFINITION_REPORT_2026-06-02.md` as a non-redundant Spanish Day 2 contrast report against the Day 1 report, focused only on deltas, corrected decisions, completed bootstrap work, validation, and remaining open questions;
- committed manifests and desktop README now point to Tauri;
- repository-wide documentation language is now Tauri-first, with the old desktop-runtime wording removed from active, status, and temporary docs;
- dependency versions in committed package manifests are pinned for the bootstrap baseline;
- live execution, launch, environment examples, and preflight are provider-aware instead of Polymarket-only;
- human clarification on 2026-06-02 says the product needs downloadable installer/distribution from landing, real Polymarket + Kalshi integrations, real live trading capability, paper only as harness/fallback, landing/GTM as sufficient commercial proof, simple human-readable reports, no screen recording requirement, and `gpt-5.5` as the stated tool target;
- Polymarket and Kalshi are now both required first-version integrations; fixtures are test artifacts only and do not count as provider integration;
- AI helper scripts now use Node `.mjs` runners instead of Bash so normal Codex workflow commands are Windows-first and do not require WSL;
- `docs/product/ASSIGNMENT_TRACEABILITY.md` now includes original-brief source status, confidence levels, a missing-brief ambiguity register, and a protocol for adding a sanitized brief, permitted excerpts, or human validation note;
- order-entry semantics use provider-currency stake/exposure terms rather than USDC-only domain names;
- monetization wording uses provider-approved paid routing/revenue share as a disabled-by-default hypothesis, not builder attribution as a canonical default.
- missing-versioned-brief risk is closed for current implementation by explicit human validation on 2026-06-02; exact brief wording remains optional archival evidence, not a blocker before Goal 01;
- historical pre-code audit language about "Polymarket-first, Kalshi deferred" is now explicitly marked superseded by the current dual-provider requirement;
- Goal 02 decimal arithmetic now uses `decimal.js` internally while preserving `DecimalString` boundaries;
- ADR-0001 live execution wording now treats real live place/cancel as an intended gated capability, not an ungated default or optional afterthought;
- Goal 04 completed the Kalshi official-access spike and implemented read-only Polymarket/Kalshi adapters; Kalshi `401`/`403` official-access failures remain explicit credential-required states rather than fake success.
- post-Goal-02 canonical state docs now point to Goal 03 instead of Goal 02;
- package bootstrap statuses no longer use `ready: true`: `core` is `domain_core_complete`, `config` is `tooling_ready`, `ui` remains `scaffold_only`, `market-data` is now `official_runtime_streaming_ready`, and `execution` is `contracts_ready`;
- `docs/status/DAILY_REPORT_2026-06-01.md` now carries a visible historical/superseded note for Polymarket-first, mock-data-first, and live-not-default language.
- `docs/ai/CONTEXT_INDEX.md` now explicitly separates current-state authority, context routing, goal templates, canonical decisions, implementation specs, support/research docs, historical/status traceability, and archive/superseded docs.
- `AGENTS.md`, `README.md`, and `docs/START_HERE_FOR_CODEX.md` now defer detailed current phase, blockers, validation, and next action to this handoff instead of restating a roadmap.
- `docs/product/ASSIGNMENT_TRACEABILITY.md`, `docs/launch/LAUNCH_PLAN.md`, `docs/launch/LAUNCH_READINESS_CHECKLIST.md`, and `docs/launch/LIVE_SMOKE_TEST.md` are explicitly marked as support/reference docs for their current role.
- historical/status files such as `docs/status/TIME_LOG.md`, Day 1/Day 2 daily reports, and pre-code reports are explicitly marked or worded as traceability artifacts rather than implementation authority.
- `docs/adr/ADR-0001-live-ready-product.md` now defers current phase and next action to this handoff while preserving the live-ready product decision.
- repository hygiene now includes expanded `.gitignore` coverage for nested dependencies, Vite/Turbo/build outputs, Tauri/Rust package artifacts, logs, local Codex state, and real env files while keeping `.env.example`, `.env.local-smoke.example`, and `pnpm-lock.yaml` commit-ready.
- `.gitattributes` and `.editorconfig` now define stable line-ending/editor conventions for the monorepo.
- obsolete empty `.agents/skills/electron-*` directories were removed so the committed local skills match the active Tauri-first direction.
- desktop terminal copy and landing bootstrap copy now resolve through `@prediction-ladder/i18n` where practical, with `en`, `es`, and `ca` catalogs, `navigator.languages` detection, `localStorage` preference persistence, root `lang` updates, and compact language selectors.
- `docs/specs/LOCALIZATION_SPEC.md` is now the canonical localization spec; architecture/dependency docs and the context index include the `packages/i18n` content-only boundary.
- localization technical debt cleanup split `packages/i18n` into typed contracts, per-locale catalogs, locale resolution, and browser-safe preference helpers; desktop and landing no longer duplicate `navigator`/`localStorage`/document language helper code.
- Goal 03 technical debt cleanup added deterministic Tauri security/config tests, aligned the main desktop minimum width to 1280px, and moved remaining visible desktop placeholder copy for connection/query/account metrics into the typed i18n catalogs.
- Goal 04A added normalized multi-venue contracts before real provider API work, keeping Polymarket/Kalshi specifics behind provider adapters or explicit `providerMetadata`.
- Goal 04A debt cleanup removed the pre-resolution `currency` requirement from market resolution requests, added localized `contracts_ready` labels, hardened the tradable-ref guard, and aligned the Start Here goal catalog.
- Goal 04 adapter work added localized `read_only_adapters_ready` labels, real read-only adapter tests, and desktop normalized snapshot consumption while preserving the Tauri security boundary.
- Goal 04 audit closed adapter/documentation inconsistencies and added `docs/status/GOAL_04_AUDIT_2026-06-03.md`; Goal 04 remains a conditional pass as REST snapshot/read-only work, not the final WebSocket/SDK runtime.
- Goal 04B added official provider runtime/WebSocket-first streaming contracts, normalizers, Tauri command boundaries, and renderer-safe projection; Goal 04C then wired the desktop market workflow into one unified multi-venue experience before Goal 05.
- Goal 05 added provider-neutral order intent validation, local disabled/paper/live-dry-run execution handling, redacted in-memory audit logging, and desktop preview/open-order/audit surfaces while keeping real live submission impossible.
- Goal 06 partial work plus audit correction added provider-neutral gated live execution contracts, mocked provider live place/cancel coverage, narrow Tauri live gate/submit/cancel commands, Tauri-owned live gate status rendering, Decimal live request checks, local approval cap enforcement, and non-committed Tauri-owned account metrics value checks.
- Goal 07 work added desktop account onboarding for Polymarket/Kalshi, Tauri-owned credential profile validation, secret-free provider onboarding/preflight commands, explicit per-provider blocker reporting, mocked Kalshi place/cancel coverage, a Polymarket provider-owned account metrics runtime for balance/allowance, open orders, positions, and conservative exposure, and a Kalshi RSA-PSS signed portfolio metrics runtime for USD balance, resting orders, positions, provider exposure, and market exposure. Real provider trading remains blocked by missing legal/local approval, credential readiness where applicable, provider live-adapter readiness, and any other live gate that cannot pass.

The active technical direction is:

- desktop: Tauri + Vite + React + TypeScript;
- landing: Vite + React + TypeScript static build;
- providers: complete Polymarket and Kalshi integration through provider-neutral ports and provider-specific adapters; fixtures are allowed only for deterministic tests;
- live execution: real gated place/cancel capability is a product target; if gates fail, show the exact blocker and do not bypass;
- domain: provider-neutral TypeScript package with deterministic tests;
- metrics: global, provider-level, and market-level PnL, available funds, open-order amount, and exposure;
- monetization: provider-approved disclosed fees/revenue share are the preferred commercial hypothesis, subscription is a fallback/private-team option, and all paid routing remains disabled by default;
- screen recordings: not evaluated and not necessary; they must not consume implementation time.

## Next action

1. Build/package the Windows installer or document the concrete blocker.
2. Then build/update the landing static page with truthful download/access flow.
3. Then perform release readiness review.
4. If a human supplies approved local credentials, legal/local approval, and explicit smoke authorization, verify provider-owned account metrics through `live_preflight_status` and run only the documented BUY-only tiny limit/GTC/post-only/non-marketable place/cancel smoke. Do not execute real orders unless every required gate passes and the human explicitly approves that smoke.

Do not start real provider order placement, paid routing, or unrelated UI polish before installer/landing/release-readiness work unless a human explicitly redirects the goal. Any authenticated provider work must remain local, secret-safe, gate-bound, and incapable of bypassing legal, geo, credential, account-metrics, risk, audit, or explicit confirmation requirements. Kalshi WebSocket authentication must remain a credential gate, not a reason to fake streaming success. Polymarket WebSocket must remain `unavailable` in the desktop command path until a safe persistent Tauri-side WebSocket transport/session is implemented.

## Vertical slice priority

```txt
repo bootstrap
  -> provider-neutral risk/order/metrics tests
  -> Tauri desktop ladder shell with provider-ready states
  -> normalized multi-venue contracts for tradable markets, books, account state, orders, fills, and settlement
  -> real Polymarket and Kalshi REST read-only order books
  -> official provider runtime with SDK/source-of-truth clients and WebSocket-first streaming
  -> unified multi-venue desktop market experience
  -> paper/live-dry-run order intent
  -> audit log
  -> gated real live place/cancel capability
  -> Windows installer/distribution artifact
  -> landing static
  -> release readiness
```

Real live execution is an intended capability and smoke target, but all live risk-increasing behavior remains blocked unless every gate is explicitly approved.

## Gates that block real live execution

- legal gate approved;
- jurisdiction/geoblock/platform eligibility pass for the selected provider;
- credential source ready;
- explicit local approval source or equivalent provider available outside committed repo state;
- max stake configured;
- max exposure configured;
- required provider/account metrics available;
- C0 clear;
- C1 human approval if required;
- paid-routing disclosure accepted if any paid routing is enabled;
- audit log enabled;
- explicit live acknowledgement.

Live must not be reachable by only flipping `ENABLE_LIVE_TRADING=true`.

## Documentation authority after cleanup

- Current state and next action: `docs/ai/context-handoff.md` only.
- Authority routing and source map: `docs/ai/CONTEXT_INDEX.md`.
- Entry pointers and guardrail summaries: `AGENTS.md`, `README.md`, and `docs/START_HERE_FOR_CODEX.md`; these defer current phase, blockers, validation, and next action to this handoff.
- Canonical product/technical/legal decisions remain in the product, architecture, legal, spec, and ADR files listed by `docs/ai/CONTEXT_INDEX.md`, including `docs/product/PROJECT_BRIEF.md`, `docs/product/MVP_SCOPE.md`, `docs/product/MONETIZATION_MODEL.md`, `docs/architecture/`, `docs/specs/`, `docs/legal/`, and active `docs/adr/` decisions.
- Support/reference docs now include `docs/product/ASSIGNMENT_TRACEABILITY.md`, `docs/launch/LAUNCH_PLAN.md`, `docs/launch/LAUNCH_READINESS_CHECKLIST.md`, `docs/launch/LIVE_SMOKE_TEST.md`, research notes, landing/desktop support docs, review roles, and source-quality notes. They support implementation but do not define the current next action.
- Historical/status traceability docs include `docs/status/`, daily reports, time logs, pre-code reports, and launch/status reports where marked. They preserve evaluator evidence and cannot override this handoff.
- Archived/superseded docs under `docs/archive/` remain non-canonical implementation context unless a human explicitly requests historical reconstruction.

## Validation status

- `pnpm typecheck`: passed on 2026-06-03 after Goal 02 audit-blocker fixes.
- `pnpm lint`: passed on 2026-06-03 after Goal 02 audit-blocker fixes.
- `pnpm test`: passed on 2026-06-03 after Goal 02 audit-blocker fixes.
- `pnpm build`: passed on 2026-06-03 after Goal 02 audit-blocker fixes.
- `pnpm ai:quality-gate`: passed on 2026-06-03 after the post-Goal-02 canonical state correction.
- `pnpm ai:quality-gate`: passed on 2026-06-03 after the documentation authority reduction; the default sandbox still cannot see `pnpm`, but the user-level pnpm path passes with the known Node.js `22.1.0` Vite engine warning.
- `pnpm ai:quality-gate`: passed on 2026-06-03 before the initial Git commit/push preparation; the default sandbox still could not see `pnpm`, but the user-level pnpm path passed with the known Node.js `22.1.0` Vite engine warning.

Environment notes:

- `corepack prepare pnpm@10.0.0 --activate` succeeded, but `corepack enable pnpm` could not write shims under `C:\Program Files\nodejs`; `pnpm@10.0.0` was installed through the user-level npm bin path instead.
- Older validation logs may mention local Node.js `22.1.0`; current Node.js is `22.22.3`, which satisfies the root engine range and Vite's supported floor.
- Tauri packaging command is configured as `pnpm package:win`, but packaging/building the Windows installer is deferred to the installer/distribution goal.

## Latest validation result

- `pnpm --filter @prediction-ladder/core typecheck`: passed on 2026-06-03 with the known Node engine warning.
- `pnpm --filter @prediction-ladder/core lint`: passed on 2026-06-03 with the known Node engine warning.
- `pnpm --filter @prediction-ladder/core test`: passed on 2026-06-03; 44 tests passed with the known Node engine warning.
- `pnpm --filter @prediction-ladder/core build`: passed on 2026-06-03 with the known Node engine warning.
- `pnpm typecheck`: passed on 2026-06-03 with the known Node engine warning.
- `pnpm lint`: passed on 2026-06-03 with the known Node engine warning.
- `pnpm test`: passed on 2026-06-03 with the known Node engine warning.
- `pnpm build`: passed on 2026-06-03 with the known Node engine warning. Vite still warns that Node.js 22.1.0 is below its preferred 22.12+ floor, but the build passes.
- Goal 02 audit blockers fixed: `decimal.js` is present in `packages/core/package.json` and `pnpm-lock.yaml`; no custom bigint decimal engine remains in `packages/core/src/index.ts`; invalid price/stake/exposure/funds/risk-limit paths return deterministic rejections; live/live-dry-run risk-increasing validation now requires `auditLogEnabled`.
- `pnpm typecheck`: passed on 2026-06-04 after the ladder-first desktop UI correction.
- `pnpm lint`: passed on 2026-06-04 after the ladder-first desktop UI correction.
- `pnpm test`: passed on 2026-06-04 after the ladder-first desktop UI correction.
- `pnpm build`: passed on 2026-06-04 after the ladder-first desktop UI correction.
- Headless Edge smoke: passed on 2026-06-04 against direct Vite React at `http://127.0.0.1:1420/`; screenshot saved to `tmp/smoke/desktop-ladder-1280.png`. The Browser plugin tool was not exposed in this turn and Playwright was not installed, so Microsoft Edge headless was used for visual smoke verification.
- `corepack pnpm --filter @prediction-ladder/core test`: passed on 2026-06-04 after Goal 05; 53 tests passed, including order-intent validation and rejection reasons.
- `corepack pnpm --filter @prediction-ladder/execution test`: passed on 2026-06-04 after Goal 05; 11 tests passed for disabled, paper, live-dry-run, live-blocked, kill-switch, audit-required, cancel, and audit-redaction behavior.
- `corepack pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-04 after Goal 05; 26 tests passed, including ladder click preview, one-click-off behavior, paper local order creation, dry-run blockers, audit rendering, and no renderer secret/provider SDK access.
- `corepack pnpm --filter @prediction-ladder/desktop typecheck`: passed on 2026-06-04 after Goal 05.
- `corepack pnpm --filter @prediction-ladder/i18n test`: passed on 2026-06-04 after Goal 05; 10 tests passed. The successful run used approved elevated execution because the sandboxed Vite/esbuild package-store path was not readable.
- `corepack pnpm typecheck`: passed on 2026-06-04 after Goal 05. Turbo emitted non-blocking no-output warnings for type-only/package tasks.
- `corepack pnpm lint`: passed on 2026-06-04 after Goal 05.
- `corepack pnpm test`: passed on 2026-06-04 after Goal 05. Workspace test totals included core 53, execution 11, desktop 26, market-data 23, i18n 10, landing 2, and config/ui smoke tests.
- `corepack pnpm build`: passed on 2026-06-04 after Goal 05; desktop and landing Vite bundles built.
- Built desktop static smoke: a Python static server serving `apps/desktop/dist` at `http://127.0.0.1:1420/` returned HTTP 200 and the expected title/assets on 2026-06-04. The in-app Browser tool was not callable after discovery, and Vite dev-server process attempts hit Windows `Path`/`PATH` environment duplication or non-persistent shell behavior, so no post-Goal-05 in-app DOM/screenshot smoke was completed.
- `git diff --check`: passed on 2026-06-04 after Goal 05 implementation.
- `corepack pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-04 after the Goal 05 audit correction; 31 desktop tests passed, including stale-preview clearing, mode/stake invalidation, and no Tauri order command coverage.
- `corepack pnpm --filter @prediction-ladder/i18n test`: passed on 2026-06-04 after the Goal 05 audit correction; 11 i18n tests passed, including regression coverage that implemented Back/Lay order-intent clicks are not described as pending Goal 05 work.
- `corepack pnpm --filter @prediction-ladder/desktop typecheck` and `corepack pnpm --filter @prediction-ladder/i18n typecheck`: passed on 2026-06-04 after the Goal 05 audit correction.
- `corepack pnpm --filter @prediction-ladder/desktop lint` and `corepack pnpm --filter @prediction-ladder/i18n lint`: passed on 2026-06-04 after the Goal 05 audit correction.
- Full `corepack pnpm typecheck`, `corepack pnpm lint`, `corepack pnpm test`, and `corepack pnpm build`: passed on 2026-06-04 after the Goal 05 audit correction.
- Pre-Goal-06 post-audit searches found no stale visible "pending Goal 05" / "later Goal 05" / "Goal 05 validation path" copy outside regression tests, and no Tauri `place_order`, `submit_order`, `order_submit`, `cancel_order`, or `order_cancel` command names in the desktop source before the gated live command boundary was added.
- `git diff --check`: passed on 2026-06-04 after the Goal 05 audit correction.
- `corepack pnpm --filter @prediction-ladder/core test`: passed on 2026-06-04 during Goal 06; 53 tests passed after approved execution because sandboxed Vitest could not resolve the pnpm Vite/esbuild store path.
- `corepack pnpm --filter @prediction-ladder/execution test`: passed on 2026-06-04 during Goal 06; 18 tests passed including mocked live place/cancel success, missing credentials, missing local approval, gate failure, provider rejection, network error, kill switch, and audit redaction.
- `corepack pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-04 during Goal 06; 33 tests passed including visible exact live blockers, explicit acknowledgement state, live button disabled until gates pass, no renderer secrets, narrow Tauri live command registration, account metrics source blocker coverage, and source-level coverage for the then-dormant Polymarket runtime selector/post-only branch.
- `cargo test`: passed on 2026-06-04 for `apps/desktop/src-tauri` after adding the Tauri Polymarket official SDK runtime branch; 18 active Rust tests passed and 1 external provider-network smoke remains ignored. Coverage includes local approval field validation, live order blocker validation, account metrics source readiness, secret-free live gate response, mocked Polymarket place success, mocked cancel success, provider rejection, network error, no provider runtime call while a live submit gate is blocked, production `provider_live_adapter_not_configured`, explicit Polymarket runtime mode selection, missing local signer material rejection, and existing market-data/TLS diagnostics.
- Full required `corepack pnpm typecheck`, `corepack pnpm lint`, `corepack pnpm test`, and `corepack pnpm build`: each failed in the sandbox on 2026-06-04 before package tasks ran because Turbo could not find the package-manager binary (`Unable to find package manager binary: cannot find binary path`). Approved elevated reruns of all four commands then passed. `corepack pnpm test` reported 13 successful test tasks, including core 53 tests, execution 18 tests, desktop 33 tests, market-data 23 tests, i18n 11 tests, landing 2 tests, and config/ui smoke tests. `corepack pnpm build` built desktop and landing Vite bundles successfully.
- `git diff --check`: passed on 2026-06-04 after the Goal 06 Polymarket runtime branch, account metrics source gate, docs, and validation updates.
- Goal 06 audit correction validation on 2026-06-04: `cargo test` passed for `apps/desktop/src-tauri` with 23 active Rust tests and 1 ignored external-provider smoke. Targeted `corepack pnpm --filter @prediction-ladder/desktop typecheck` passed, targeted desktop tests passed with 37 tests after approved execution, and targeted core/execution tests remained green with 53 and 18 tests. Full required `corepack pnpm typecheck`, `corepack pnpm lint`, `corepack pnpm test`, and `corepack pnpm build` first failed in the sandbox because Turbo could not find the package-manager binary, then passed with approved execution. The full test run reported 13 successful tasks including desktop 37, core 53, execution 18, market-data 23, i18n 11, landing 2, and config/ui smoke tests. `git diff --check` passed after the implementation/docs updates.
- `tauri-ux-quality-review`: `CONDITIONAL_PASS` on 2026-06-04. No renderer secret path, no hidden live hotkey, persistent mode/gate/kill-switch/audit surfaces remain visible, and live submit is not exposed from React. The follow-up to consume the secret-free `live_gate_status` command is now closed by the Goal 06 audit correction; the visible gate panel receives Tauri-sourced local approval, credential, account-metrics, and bridge blocker reasons.
- Renderer smoke note after Goal 06 partial implementation: no new browser DOM/screenshot smoke was run for the Polymarket runtime branch because this follow-up changed command/runtime plumbing, gate modeling, tests, and docs rather than visible layout. The desktop production build and desktop model/security tests passed.
- Goal 07 account-metrics follow-up validation on 2026-06-04: `cargo test` passed for `apps/desktop/src-tauri` with 30 active Rust tests and 1 ignored external-provider smoke. Coverage includes secret-free provider onboarding status, Polymarket seed-like input rejection, local secure provider profile persistence without returning credential paths/key material, secret-free `live_preflight_status` blockers, provider-owned Polymarket account metrics readiness without a local metrics file, mocked Polymarket place/cancel success, mocked Kalshi place/cancel success, provider rejection/network-error mapping, stale/missing account metrics blockers, marketable-order blockers, exposure/funds blockers, and no provider call while Tauri-side gates reject. Targeted `corepack pnpm --filter @prediction-ladder/desktop typecheck`, `lint`, and `build` passed in the sandbox. Targeted desktop Vitest initially failed in the sandbox before test execution because Vite 7 could not resolve `esbuild/index.js`; approved `corepack pnpm install`, approved `corepack pnpm rebuild esbuild`, and approved `corepack pnpm install --force --offline` did not make the sandboxed direct test invocation pass. The required root `corepack pnpm typecheck`, `lint`, `test`, and `build` each failed in the sandbox before package tasks ran because Turbo could not find the package-manager binary, then all passed under approved execution. The approved root test run completed 13 successful tasks, including desktop Vitest with 43 passing tests, core 53, execution 18, market-data 23, i18n 11, landing 2, config 1, and ui 1. `git diff --check` passed.
- Renderer HTTP smoke for Goal 07 on 2026-06-04: foreground `corepack pnpm --filter @prediction-ladder/desktop dev:vite -- --host 127.0.0.1 --port 1420` started Vite 8 successfully, and a parallel `Invoke-WebRequest http://127.0.0.1:1420/` returned `status=200`, HTML length `567`, and the React root element. The command was killed by timeout afterward, and a final probe confirmed port 1420 was no longer listening. The in-app Browser tool was not callable after tool discovery in this session, so no browser screenshot/DOM inspection was captured for the new onboarding panel.
- `tauri-ux-quality-review`: `CONDITIONAL_PASS` for the earlier Goal 07 account-metrics follow-up on 2026-06-04. The safety posture remains strong: live state, kill switch, audit/gate surfaces, and preflight blockers stay visible; the renderer still has no private-key/API-secret/auth-header/signature/signed-payload/full-account-id path and no `order_submit_live`/`order_cancel` client. Top issues before demo are no fresh browser/Tauri visual inspection of the denser onboarding/right rail at 1280x720 and 1366x768 in that slice, real credential/network smoke for provider-owned metrics not run, and provider live-adapter readiness still gated. Live trading UX remains blocked until all legal/geo/credential/local approval/account metrics/provider adapter/risk/audit/acknowledgement/non-marketable gates pass and the human explicitly approves the live smoke.
- Goal 07 Kalshi signed metrics validation on 2026-06-04: `pnpm typecheck`, `pnpm lint`, `pnpm test`, and `pnpm build` passed under approved execution because plain `pnpm` is not visible in the sandbox. The final root test run completed 13 successful tasks, including desktop Vitest with 43 passing tests, core 53, execution 18, market-data 23, i18n 11, landing 2, config 1, and ui 1. Targeted provider/desktop/Tauri validation also passed: `pnpm --filter @prediction-ladder/desktop test` passed with 43 tests, `pnpm --filter @prediction-ladder/market-data test` passed with 23 tests, and `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` passed with 35 active Rust tests plus 1 ignored external-provider smoke. Rust coverage now includes Kalshi RSA key validation, Kalshi signature path construction using `/trade-api/v2/...` without query parameters, Kalshi account metrics normalization, Kalshi provider-owned metrics status in USD without a local metrics file, mocked Kalshi place/cancel success behind gates, provider rejection/network mapping, and secret-free onboarding/preflight paths. `git diff --check` passed. No real provider credentials were supplied, no real credential/network smoke was run, and no real orders were executed.
- `tauri-ux-quality-review`: `CONDITIONAL_PASS` for the Goal 07 Kalshi signed metrics closure on 2026-06-04. The implementation changes command/runtime safety rather than visible layout: live state, kill switch, audit/gate surfaces, and preflight blockers remain visible; React still has no private-key/API-secret/auth-header/signature/signed-payload/full-account-id path; the desktop security test now explicitly checks renderer sources while allowing Tauri-owned `RsaPrivateKey` handling in the main process. Fixes before demo: run a fresh browser/Tauri visual inspection of the onboarding/right rail at 1280x720 and 1366x768 when packaging/installer work starts; run real credential/network preflight only if the human supplies approved local credentials and legal/local approval; keep Kalshi live execution blocked until an authenticated live adapter exists. Handoff is updated.
- Goal 07 onboarding wizard UX correction on 2026-06-04: the fixed right-rail credential fields were replaced with compact provider status cards that open a modal wizard. The wizard shows official Polymarket and Kalshi credential URLs as visible text without active remote webview navigation, collects only local signer/API-key-ID/.key-file references inside the modal, and keeps live preflight status visible in the rail. Browser DOM verification at `http://127.0.0.1:1420/` confirmed zero inputs in provider rail cards, `Set up Polymarket` and `Set up Kalshi` actions, a centered 680px modal at the 1280x720 viewport, visible Polymarket/Kalshi official URLs, Polymarket local signer reference input only inside the wizard, Kalshi API Key ID plus local `.key` inputs only inside the wizard, and no rail credential inputs. Targeted desktop security regression passed with 6 tests. Final `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, and `git diff --check` passed under approved execution where needed. `tauri-ux-quality-review` is `PASS` for this UX correction: no new live-trading UX blocker was introduced, no renderer secret path or live submit/cancel client was added, and live remains blocked by Tauri-owned preflight and provider adapter gates.
- Goal 07 legal onboarding follow-up validation on 2026-06-05: targeted `pnpm --filter @prediction-ladder/desktop test` passed with 48 desktop tests, targeted `corepack pnpm --filter @prediction-ladder/desktop typecheck` and `corepack pnpm --filter @prediction-ladder/desktop lint` passed, targeted `cargo test` for `apps/desktop/src-tauri` passed with 40 active Rust tests and 1 ignored external provider-network smoke, targeted `corepack pnpm --filter @prediction-ladder/i18n test` passed with 13 tests under approved execution because the sandbox could not read `esbuild/package.json` through the pnpm Vite/Vitest dependency junction, and targeted `corepack pnpm --filter @prediction-ladder/market-data test` passed with 23 tests under the same approved execution pattern. Required root `corepack pnpm typecheck`, `corepack pnpm lint`, `corepack pnpm test`, and `corepack pnpm build` all passed under approved execution after sandboxed attempts failed before package tasks because Turbo could not find the package-manager binary. The root test run reported 13 successful tasks, including desktop 48, i18n 13, core 53, execution 18, market-data 23, landing 2, config 1, and ui 1. `git diff --check` passed, `http://127.0.0.1:1420/` returned HTTP 200, and the built desktop bundle contains the legal approval surface. Rust coverage now includes legal approval submit rejection for missing fields/checks, app-managed local approval write without secrets, and local approval satisfying legal/live-enable approval without env vars while remaining blocked by missing credentials/account/provider gates. Desktop coverage includes localized legal approval UI, no renderer live submit/cancel client, legal/local/geo gate projection from Tauri status, and secret-free legal command fallbacks.
- `tauri-ux-quality-review`: `CONDITIONAL_PASS` for the 2026-06-05 legal onboarding follow-up. The new panel/modal keeps the terminal risk rail pattern, mode/gate/kill-switch/preflight visibility remains intact, React still has no private-key/API-secret/auth-header/signature/signed-payload/full-account-id path, and the checklist is localized. Remaining UX watch item: the legal checklist is dense at 1280x720, but the modal is scrollable, text wraps, and it is safer to show all declarations than to hide them behind vague copy.
- Goal 07 legal onboarding UX simplification validation on 2026-06-05: the legal modal now displays the required declarations as review text plus one explicit acknowledgement checkbox. That acknowledgement expands into the full declaration boolean payload for `legal_approval_submit`, so Tauri still validates every C0/C1/no-bypass/no-custody/audit/no-deposit/first-smoke declaration and returns exact blockers if the acknowledgement is missing. Successful provider credential import now opens this legal/local approval flow automatically for the same provider, including the Polymarket Magic clipboard import path. Targeted validation passed: `pnpm --filter @prediction-ladder/desktop typecheck`, `pnpm --filter @prediction-ladder/desktop test` (48 tests), `pnpm --filter @prediction-ladder/desktop lint`, approved `corepack pnpm --filter @prediction-ladder/i18n test` (13 tests after sandbox `esbuild` resolution failure), `cargo test` in `apps/desktop/src-tauri` (40 active tests plus 1 ignored external smoke), and `git diff --check`. Required root gates passed under approved execution after the sandboxed Turbo attempts failed before package tasks with `cannot find binary path`: `corepack pnpm typecheck`, `corepack pnpm lint`, `corepack pnpm test` (13 successful tasks), and `corepack pnpm build`. `tauri-ux-quality-review` is `CONDITIONAL_PASS`: the flow is less click-heavy without hiding blocker semantics, live submit/cancel clients remain absent from React, secret-like strings remain absent from renderer command clients, and the remaining watch item is no fresh browser/Tauri visual inspection in this slice.
- Goal 07 first-live-smoke closure validation on 2026-06-05: Polymarket live runtime is configured by default in Tauri while Kalshi live remains blocked; React now has only secret-free Tauri bridges for `order_submit_live` and `order_cancel`; the right rail exposes a disabled-by-default "submit live smoke and cancel" action that requires live mode, a validated BUY/non-marketable limit/GTC preview, selected-provider preflight ready, fresh official book, app-managed legal/local approval, credential readiness, provider-owned account metrics, audit enabled, kill switch off, and explicit acknowledgement. Targeted validation passed: `pnpm --filter @prediction-ladder/desktop typecheck`, `pnpm --filter @prediction-ladder/desktop test` (49 tests), and `cargo test` in `apps/desktop/src-tauri` (40 active tests plus 1 ignored external provider-network smoke). Required root gates passed: `pnpm typecheck`, `pnpm lint`, approved `pnpm test` after the sandbox stopped finding the pnpm shim (13 successful tasks, including desktop 49, core 53, execution 18, market-data 23, i18n 13, landing 2, config 1, ui 1), approved `pnpm build`, and `git diff --check`. `tauri-ux-quality-review` is `CONDITIONAL_PASS`: Tauri safety posture and visible gates remain strong, but no fresh real Tauri-window visual inspection at 1280x720/1366x768 and no real credential/provider smoke were run in this slice. No real orders were executed.
- `node --preserve-symlinks-main scripts/ai/check-no-fake-gates.mjs`: passed; no fake-positive quality gate scripts detected.
- `node --preserve-symlinks-main scripts/ai/session-start.mjs`: passed after follow-up cleanup; it reads `docs/ai/context-handoff.md` for the current next action.
- `pnpm --version`: passed with approval/user-level pnpm path; version is `10.0.0`.
- `pnpm typecheck`: passed with the known Node engine warning.
- `pnpm lint`: passed with the known Node engine warning.
- `pnpm test`: passed with the known Node engine warning.
- `pnpm build`: passed with the known Node engine warning.
- `node --preserve-symlinks-main scripts/ai/quality-gate.mjs`: failed in the default sandbox because `pnpm` was not visible on PATH; passed with approval/user-level pnpm path and internally ran `pnpm typecheck`, `pnpm lint`, `pnpm test`, and `pnpm build`.
- Optional `pnpm --filter @prediction-ladder/desktop build`: passed with the known Node engine warning.
- Optional `pnpm package:win`: not run in this block; native `cargo check` now passes through `C:\Users\1\.cargo\bin\cargo.exe`, but installer packaging remains deferred to the installer/distribution goal.
- Browser DOM smoke passed for the landing page at `http://127.0.0.1:5174/`: title `Prediction Ladder`, one `Prediction Ladder` heading, and three trust notes.
- Browser DOM smoke passed for the desktop renderer at `http://127.0.0.1:1420/`: title `Prediction Ladder Desktop`, one `Prediction Ladder` heading, four safety checks, and disabled execution mode visible.
- Browser screenshot capture timed out in the browser backend, so the smoke result is DOM-based.
- `node --preserve-symlinks-main scripts/ai/live-preflight.mjs`: failed as expected because `LEGAL_GATE_STATUS` is not approved.
- Documentation search found no active Bash `.sh` workflow scripts. Remaining `.sh` text only appears inside external `skills.sh` URLs.
- Active documentation now treats Polymarket and Kalshi as required first-version integrations. Fixture/mock data is acceptable only for tests and must not be presented as product integration.
- The standard pnpm quality gate is no longer blocked; pnpm is available through the user-level npm installation.
- Bash/WSL is no longer required for the normal `pnpm ai:*` helper scripts; they now run through Node `.mjs` files.
- Git status is available in this workspace; change review should use `git status` plus direct file/search validation while preserving unrelated uncommitted work.
- Assignment traceability now closes the missing-versioned-brief issue for implementation through explicit human validation; exact brief text remains optional archival evidence if it becomes available later.
- Human brief-clarification answers have been incorporated into assignment traceability, project brief, MVP scope, launch plan, live execution spec, GTM docs, legal model, and AI/review workflow docs.
- Documentation search found intentional active references to `gpt-5.5` and screen recordings; those references document human clarification and keep recordings out of the critical implementation path.
- `pnpm ai:quality-gate`: first failed in the default sandbox because `pnpm` was not visible on PATH; passed with approval/user-level pnpm after the canonical state correction and internally ran `pnpm typecheck`, `pnpm lint`, `pnpm test`, and `pnpm build`.
- Final `pnpm ai:quality-gate` rerun after the handoff update also passed with the known Node.js `22.1.0` Vite engine warning.
- An earlier uncached correction-pass run emitted non-blocking Turborepo "no output files found" cache warnings for tasks that intentionally emit no build artifacts.
- Documentation authority checks passed on 2026-06-03: targeted searches found the detailed Goal 03 next implementation block only in this handoff; remaining active next-action/current-phase mentions are pointers to this handoff or explicitly historical/support wording; `docs/archive/README.md` still marks archived files as non-canonical.
- Git ignore checks passed before the initial commit: `.codex/`, `.local/`, `.turbo/`, nested `node_modules/`, app `dist/` outputs, and real `.env` files are ignored; `.env.example`, `.env.local-smoke.example`, `pnpm-lock.yaml`, source, docs, scripts, and active `.agents/skills` are commit-ready.
- `pnpm install --offline`: passed on 2026-06-03 after adding the local `@prediction-ladder/i18n` workspace package; no external dependencies were added. The known Node.js `22.1.0` engine warning remains.
- `pnpm typecheck`: passed on 2026-06-03 after the localization implementation with the known Node engine warning.
- `pnpm lint`: passed on 2026-06-03 after the localization implementation with the known Node engine warning.
- `pnpm test`: passed on 2026-06-03 after the localization implementation with the known Node engine warning; `@prediction-ladder/i18n` added 5 passing locale/catalog tests.
- `pnpm build`: passed on 2026-06-03 after the localization implementation with the known Node/Vite warning.
- `pnpm typecheck`: passed on 2026-06-03 after localization debt cleanup with the known Node engine warning.
- `pnpm lint`: passed on 2026-06-03 after localization debt cleanup with the known Node engine warning.
- `pnpm test`: passed on 2026-06-03 after localization debt cleanup with the known Node engine warning; `@prediction-ladder/i18n` now has 9 passing tests covering catalog parity and browser preference helpers.
- `pnpm build`: passed on 2026-06-03 after localization debt cleanup with the known Node/Vite warning.
- `pnpm --filter @prediction-ladder/desktop typecheck`: passed on 2026-06-03 after the Goal 03 terminal shell implementation with the known Node engine warning.
- `pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-03 after the Goal 03 terminal shell implementation; 4 deterministic desktop model tests passed.
- `pnpm --filter @prediction-ladder/desktop lint`: passed on 2026-06-03 after the Goal 03 terminal shell implementation with the known Node engine warning.
- `pnpm --filter @prediction-ladder/desktop build`: passed on 2026-06-03 after the Goal 03 terminal shell implementation with the known Node/Vite warning.
- `pnpm --filter @prediction-ladder/i18n typecheck`: passed on 2026-06-03 after adding terminal shell copy keys with the known Node engine warning.
- `pnpm --filter @prediction-ladder/i18n test`: passed on 2026-06-03 after adding terminal shell copy keys; 9 catalog/locale tests passed.
- `pnpm --filter @prediction-ladder/i18n lint`: passed on 2026-06-03 after adding terminal shell copy keys with the known Node engine warning.
- Browser DOM/layout smoke passed for the desktop renderer at `http://127.0.0.1:1420/` after Goal 03: Polymarket and Kalshi present, live blocked visible, one-click off, kill switch visible, no submit path visible, unknown metrics visible, 3 terminal panels present, 14 placeholder ladder rows present, 10 disabled trading/action controls present, and fixed top/bottom strips measured correctly with internal panel scrolling. Browser screenshot capture timed out in the browser backend, so the smoke result remains DOM/layout based.
- `pnpm typecheck`: passed on 2026-06-03 after Goal 03 with the known Node engine warning and non-blocking Turbo no-output warnings for type-only package builds.
- `pnpm lint`: passed on 2026-06-03 after Goal 03 with the known Node engine warning.
- `pnpm test`: passed on 2026-06-03 after Goal 03 with the known Node engine warning; the workspace suite includes 63 passing Vitest tests across core, desktop, i18n, landing, and scaffold packages.
- `pnpm build`: passed on 2026-06-03 after Goal 03 with the known Node/Vite warning; desktop and landing Vite bundles built successfully.
- `pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-03 after Goal 03 debt cleanup; desktop now has 7 passing tests, including 3 deterministic Tauri security/config tests.
- `pnpm typecheck`: passed on 2026-06-03 after Goal 03 debt cleanup with the known Node engine warning and a non-blocking Turbo no-output warning for type-only package builds.
- `pnpm lint`: passed on 2026-06-03 after Goal 03 debt cleanup with the known Node engine warning.
- `pnpm test`: passed on 2026-06-03 after Goal 03 debt cleanup with the known Node engine warning; the workspace suite now includes 66 passing Vitest tests.
- `pnpm build`: passed on 2026-06-03 after Goal 03 debt cleanup with the known Node/Vite warning; desktop and landing Vite bundles built successfully.
- `Invoke-WebRequest http://127.0.0.1:1420/`: returned HTTP 200 on 2026-06-03 after Goal 03 debt cleanup.
- In-app browser DOM smoke could not be rerun after Goal 03 debt cleanup because the Browser plugin route for the current in-app browser session returned `No Codex browser route is available`; this is a tool/session limitation, not a failed app validation. The prior post-Goal-03 DOM/layout smoke remains the latest successful browser DOM smoke.
- `pnpm --filter @prediction-ladder/core test`: passed on 2026-06-03 after Goal 04A; core now has 47 passing tests covering tradable refs, order-book normalization, normalized multi-venue books, and unknown account-state contracts.
- `pnpm --filter @prediction-ladder/market-data test`: passed on 2026-06-03 after Goal 04A; market-data now has 3 passing tests covering contracts-ready status, fixture-vs-live source distinction, adapter ports, and explicit adapter errors.
- `pnpm --filter @prediction-ladder/execution test`: passed on 2026-06-03 after Goal 04A; execution now has 3 passing tests covering contracts-ready status, blocked place/cancel ports, and credential-required account state without fake balances.
- `pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-03 after Goal 04A; desktop still has 7 passing tests and now verifies the provider switch uses canonical `PROVIDER_IDS` from `@prediction-ladder/core`.
- `pnpm install --offline`: passed on 2026-06-03 after adding workspace dependencies from `packages/market-data` and `packages/execution` to `@prediction-ladder/core`; no external downloads were needed.
- `pnpm typecheck`: initially failed on 2026-06-03 before the debt cleanup because `MarketResolveRequest` still inherited `MarketRef` and required `currency`; after the immediate test fix it passed, and the later debt cleanup removed that pre-resolution `currency` requirement.
- `pnpm lint`: passed on 2026-06-03 after Goal 04A with the known Node engine warning.
- `pnpm test`: passed on 2026-06-03 after Goal 04A with the known Node engine warning; the workspace suite now includes 69 passing tests.
- `pnpm build`: passed on 2026-06-03 after Goal 04A with the known Node/Vite warning; desktop and landing Vite bundles built successfully.
- `pnpm --filter @prediction-ladder/core test`: passed on 2026-06-03 after Goal 04A debt cleanup; core now has 48 passing tests including a full-validation `isTradableMarketRef` regression.
- `pnpm --filter @prediction-ladder/market-data test`: passed on 2026-06-03 after Goal 04A debt cleanup; `MarketResolveRequest` no longer requires `currency` before adapter resolution.
- `pnpm --filter @prediction-ladder/i18n test`: passed on 2026-06-03 after Goal 04A debt cleanup; i18n now has 10 passing tests including `contracts_ready` label coverage.
- `pnpm typecheck`: passed on 2026-06-03 after Goal 04A debt cleanup with the known Node engine warning.
- `pnpm lint`: passed on 2026-06-03 after Goal 04A debt cleanup with the known Node engine warning.
- `pnpm test`: passed on 2026-06-03 after Goal 04A debt cleanup with the known Node engine warning; the workspace suite now includes 70 passing tests.
- `pnpm build`: passed on 2026-06-03 after Goal 04A debt cleanup with the known Node/Vite warning; desktop and landing Vite bundles built successfully.
- `pnpm install --offline`: passed on 2026-06-03 after adding the desktop dependency on `@prediction-ladder/market-data`; no external downloads were needed.
- `pnpm --filter @prediction-ladder/core test`: passed on 2026-06-03 after Goal 04; 48 tests passed.
- `pnpm --filter @prediction-ladder/market-data test`: passed on 2026-06-03 after the initial Goal 04 implementation; 12 tests passed covering Polymarket/Kalshi normalization into shared ladder rows, discovery, configured fallback source distinction, malformed payloads, stale data, empty liquidity, unsupported markets, missing outcomes, invalid ticks, network errors, provider errors, and Kalshi credential-required responses.
- `pnpm --filter @prediction-ladder/execution test`: passed on 2026-06-03 after Goal 04; 3 tests passed and execution remains contract-only/blocked.
- `pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-03 after Goal 04; 9 tests passed, including normalized live snapshot consumption and fixture blocking with order submission still disabled.
- `pnpm typecheck`: passed on 2026-06-03 after Goal 04 with the known Node engine warning and non-blocking Turbo no-output warnings for type-only package builds.
- `pnpm lint`: passed on 2026-06-03 after Goal 04 with the known Node engine warning.
- `pnpm test`: passed on 2026-06-03 after Goal 04 with the known Node engine warning; the workspace suite now includes 86 passing tests.
- `pnpm build`: passed on 2026-06-03 after Goal 04 with the known Node/Vite warning; desktop and landing Vite bundles built successfully.
- `pnpm --filter @prediction-ladder/market-data test`: passed on 2026-06-03 after Goal 04 audit fixes; 13 tests passed, adding the regression that Kalshi markets with missing `status` are not treated as open.
- `pnpm --filter @prediction-ladder/core test`: passed on 2026-06-03 during Goal 04 audit; 48 tests passed.
- `pnpm --filter @prediction-ladder/execution test`: passed on 2026-06-03 during Goal 04 audit; 3 tests passed.
- `pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-03 during Goal 04 audit; 9 tests passed.
- `pnpm typecheck`: passed on 2026-06-03 during Goal 04 audit with the known Node engine warning and non-blocking Turbo no-output warning for type-only package builds.
- `pnpm lint`: passed on 2026-06-03 during Goal 04 audit with the known Node engine warning.
- `pnpm test`: passed on 2026-06-03 during Goal 04 audit with the known Node engine warning; the workspace suite now includes 87 passing tests.
- `pnpm build`: passed on 2026-06-03 during Goal 04 audit with the known Node/Vite warning; desktop and landing Vite bundles built successfully.
- `git diff --check`: passed on 2026-06-03 during Goal 04 audit.
- `pnpm --filter @prediction-ladder/core test`: passed on 2026-06-03 after Goal 04B; 48 tests passed.
- `pnpm --filter @prediction-ladder/market-data test`: passed on 2026-06-03 after Goal 04B; 22 tests passed covering official runtime choices, Polymarket/Kalshi WebSocket normalization, out-of-order updates, reconnect state, stale timeout, malformed messages, Kalshi auth-required handshake, REST recovery/fallback, and renderer-safe normalized projection.
- `pnpm --filter @prediction-ladder/execution test`: passed on 2026-06-03 after Goal 04B; 3 tests passed and execution remains contract-only/blocked.
- `pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-03 after Goal 04B; 11 tests passed covering the full provider-runtime connection-state vocabulary and Tauri market-data command boundary registration without renderer secret exposure.
- `pnpm typecheck`: initially failed during Goal 04B on strict optional-property handling in the new streaming runtime; after fixing optional `previousSnapshot`/`providerMetadata` construction it passed with the known Node engine warning.
- `pnpm lint`: passed on 2026-06-03 after Goal 04B with the known Node engine warning.
- `pnpm test`: passed on 2026-06-03 after Goal 04B with the known Node engine warning; the workspace suite now includes 98 passing tests.
- `pnpm build`: passed on 2026-06-03 after Goal 04B with the known Node/Vite warning; desktop and landing Vite bundles built successfully.
- `git diff --check`: passed on 2026-06-03 after Goal 04B.
- `pnpm --filter @prediction-ladder/core test`: passed on 2026-06-03 after the Goal 04B audit fixes; 48 tests passed.
- `pnpm --filter @prediction-ladder/market-data test`: passed on 2026-06-03 after the Goal 04B audit fixes; 23 tests passed, adding provider-specific error projection coverage.
- `pnpm --filter @prediction-ladder/execution test`: passed on 2026-06-03 after the Goal 04B audit fixes; 3 tests passed.
- `pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-03 after the Goal 04B audit fixes; 12 tests passed, adding provider-specific desktop error-state coverage.
- `pnpm typecheck`: initially failed during the Goal 04B audit because a helper returned `MarketDataResult<unknown>` from a runtime search error path; after narrowing that branch to the error-only `Result` shape, it passed with the known Node engine warning.
- `pnpm lint`: passed on 2026-06-03 after the Goal 04B audit fixes with the known Node engine warning.
- `pnpm test`: passed on 2026-06-03 after the Goal 04B audit fixes with the known Node engine warning; the workspace suite now includes 100 passing tests.
- `pnpm build`: passed on 2026-06-03 after the Goal 04B audit fixes with the known Node/Vite warning; desktop and landing Vite bundles built successfully.
- `git diff --check`: passed on 2026-06-03 after the Goal 04B audit fixes.
- `pnpm --filter @prediction-ladder/core test`: passed on 2026-06-03 after Goal 04C; 48 tests passed.
- `pnpm --filter @prediction-ladder/market-data test`: passed on 2026-06-03 after Goal 04C; 23 tests passed.
- `pnpm --filter @prediction-ladder/execution test`: passed on 2026-06-03 after Goal 04C; 3 tests passed.
- `pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-03 after Goal 04C; 17 tests passed, adding unified search, order-book selection, subscription-state, fixture-blocking, secret-stripping, and no-submit-path coverage.
- `pnpm typecheck`: passed on 2026-06-03 after Goal 04C with the known Node engine warning.
- `pnpm lint`: passed on 2026-06-03 after Goal 04C with the known Node engine warning.
- `pnpm test`: passed on 2026-06-03 after Goal 04C with the known Node engine warning; the workspace suite now includes 105 passing tests.
- `pnpm build`: passed on 2026-06-03 after Goal 04C with the known Node/Vite warning; desktop and landing Vite bundles built successfully.
- `git diff --check`: passed on 2026-06-03 after Goal 04C.
- `cargo --version`: failed earlier on 2026-06-03 because `cargo` was not on PATH in the active shell; after Rust installation it is available at `C:\Users\1\.cargo\bin`.
- `pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-03 after the Goal 04C audit fixes; 18 tests passed, adding the selected-venue snapshot-vs-stream state regression. A later post-audit correction changed empty search into the all-venues browse path.
- `pnpm typecheck`: passed on 2026-06-03 after the Goal 04C audit fixes with the known Node engine warning.
- `pnpm lint`: passed on 2026-06-03 after the Goal 04C audit fixes with the known Node engine warning.
- `pnpm test`: passed on 2026-06-03 after the Goal 04C audit fixes with the known Node engine warning; the workspace suite now includes 106 passing tests.
- `pnpm build`: passed on 2026-06-03 after the Goal 04C audit fixes with the known Node/Vite warning; desktop and landing Vite bundles built successfully.
- `git diff --check`: passed on 2026-06-03 after the Goal 04C audit fixes.
- `cargo check`: initially failed after Rust installation because Tauri required `apps/desktop/src-tauri/icons/icon.ico`; after adding the local Windows icon asset, `cargo check` passed for `apps/desktop/src-tauri`.
- Node.js was upgraded from `22.1.0` to `22.22.3` on 2026-06-03. Follow-up `pnpm` gates should no longer show the prior Vite Node-version warning.
- `Invoke-WebRequest http://127.0.0.1:1420/`: returned HTTP 200 on 2026-06-03 after Goal 04C.
- In-app browser verification at `http://127.0.0.1:1420/` passed for the plain-browser dev-server surface before the final label cleanup: terminal shell, market rail, ladder workspace, risk rail, search input, venue health, stream status, kill switch, one-click-off state, and disabled execution controls were present. Searching `fed` in the plain browser returned explicit `unavailable` / "Tauri command bridge is unavailable" state, as expected outside a Tauri webview, and did not fake provider success.
- `pnpm --filter @prediction-ladder/desktop typecheck`: passed on 2026-06-03 after the Goal 04C all-markets browse and Polymarket SDK correction.
- `pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-03 after the Goal 04C all-markets browse correction; 18 desktop tests passed, including the empty-query browse regression.
- `cargo check`: passed on 2026-06-03 for `apps/desktop/src-tauri` after adding `polymarket_client_sdk_v2` with `gamma`/`clob` features. The first SDK compile was long because native dependencies were built, but the final check completed successfully.
- `pnpm typecheck`: passed on 2026-06-03 after the Goal 04C correction.
- `pnpm lint`: initially failed because generated Rust/Tauri files under `apps/desktop/src-tauri/target` were linted after `cargo check`; after adding `target/**` and `**/target/**` to `eslint.config.mjs`, `pnpm lint` passed.
- `pnpm test`: passed on 2026-06-03 after the Goal 04C correction. Turbo reported 12 successful test tasks, with desktop at 18 passing tests.
- `pnpm build`: passed on 2026-06-03 after the Goal 04C correction. Turbo replayed an old cached landing build log that mentioned Node.js 22.1.0, but current `node --version` returns `v22.22.3`.
- `cargo test`: passed again on 2026-06-03 for `apps/desktop/src-tauri`; 7 Rust tests pass and 1 external-network smoke remains ignored by default. The added unit coverage verifies partial provider blockers are named in the unified search message and Kalshi TLS/certificate transport failures receive an explicit DNS/proxy/network-filtering diagnostic.
- Explicit ignored Rust provider smoke with approved network access passed on 2026-06-03: `market_search` returned `status=connected`, `markets=10`, `provider_states=polymarket:connected,kalshi:disconnected`. Direct `curl.exe` checks to both documented Kalshi hosts failed TLS hostname/principal verification on this machine, so Kalshi must remain an explicit disconnected provider state here rather than bypassing certificate validation.
- Final post-correction gates on 2026-06-03: `corepack pnpm typecheck`, `corepack pnpm lint`, `corepack pnpm test`, and `corepack pnpm build` passed with approved elevated execution because Turbo cannot resolve the workspace package-manager binary inside the sandbox. `cargo test` for `apps/desktop/src-tauri` passed with 7 tests and 1 ignored external-network smoke. The explicit ignored provider smoke passed with approved network access after the diagnostic change and still returned `status=connected`, `markets=10`, `provider_states=polymarket:connected,kalshi:disconnected`. `corepack pnpm --filter @prediction-ladder/desktop test` passed with 20 desktop tests. `git diff --check` passed.
- `pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-03 after the large Goal 04C audit correction; desktop now has 20 passing tests covering empty browse, direct provider filters, load-more pagination with offset/cursor, order-book selection, subscription-state visibility, fixture blocking, secret stripping, and no submit path.
- Browser/in-app renderer smoke at `http://127.0.0.1:1420/` passed on 2026-06-03 after the large Goal 04C audit correction: the plain-browser surface shows the unified market rail, all/Poly/Kal filters with complete accessible labels and no text truncation, the Stream label, explicit Tauri command bridge unavailable state, visible ladder/risk/gate surfaces, and no renderer-side provider API success.
- `pnpm typecheck`: passed on 2026-06-03 after the large Goal 04C audit correction. The Codex sandbox could not resolve the workspace package-manager binary for Turbo, so the successful gate was run with approved elevated execution through `corepack pnpm`.
- `pnpm lint`: passed on 2026-06-03 after the large Goal 04C audit correction with approved elevated execution.
- `pnpm test`: passed on 2026-06-03 after the large Goal 04C audit correction with approved elevated execution. Turbo reported 12 successful test tasks; desktop has 20 passing tests and i18n has 10 passing tests.
- `pnpm build`: passed on 2026-06-03 after the large Goal 04C audit correction with approved elevated execution; desktop and landing Vite bundles built successfully.
- `git diff --check`: passed on 2026-06-03 after the large Goal 04C audit correction.
- `pnpm --filter @prediction-ladder/desktop test`: passed on 2026-06-03 after adding search-adjacent per-venue diagnostics; desktop now has 21 passing tests, including partial-provider-blocker diagnostic propagation.
- Browser/in-app renderer DOM/layout smoke at `http://127.0.0.1:1420/` passed on 2026-06-03 after adding per-venue diagnostics: the plain-browser surface showed Polymarket and Kalshi diagnostic rows under the unified search summary, all/Poly/Kal filters with accessible labels, the `Stream` label instead of `WebSocket`, explicit Tauri command bridge unavailable state, no renderer-side provider API success, and no horizontal overflow at 1280px. Screenshot capture timed out in the Browser backend, so this smoke is DOM/layout evidence rather than image evidence.
- Final per-venue-diagnostics gates on 2026-06-03: `corepack pnpm typecheck`, `corepack pnpm lint`, `corepack pnpm test`, and `corepack pnpm build` passed with approved elevated execution; `git diff --check` passed. The temporary Vite smoke server was stopped and its workspace logs were removed.
- Follow-up per-venue result-count observability on 2026-06-03: the direct all/Polymarket/Kalshi filter buttons and the search-adjacent diagnostic strip now show visible result counts in addition to status, connection mode, freshness, page state, and latest message/error. Browser DOM/layout smoke at `http://127.0.0.1:1420/` confirmed both Polymarket and Kalshi rows include `Resultados: 0` in the plain-browser unavailable state, the filter buttons expose count-bearing accessible labels (`Todos los venues, Resultados: 0`, `Polymarket, Resultados: 0`, `Kalshi, Resultados: 0`), the `Stream` label is present, `WebSocket` text is absent, and there is no horizontal overflow. `corepack pnpm --filter @prediction-ladder/desktop typecheck`, `corepack pnpm --filter @prediction-ladder/desktop test`, `corepack pnpm typecheck`, and `corepack pnpm lint` passed after this follow-up. `corepack pnpm test` and `corepack pnpm build` could not be rerun after the final filter-count button adjustment because the automatic approval review rejected elevated execution after hitting the usage limit; the latest full `test`/`build` gates before that small UI adjustment had passed. `git diff --check` passed after the documentation correction. The local Vite server is currently available at `http://127.0.0.1:1420/` for manual review.
- Goal 07 desktop validation UX follow-up validation on 2026-06-05: `pnpm --filter @prediction-ladder/desktop typecheck`, `pnpm --filter @prediction-ladder/desktop test` (51 tests), `pnpm --filter @prediction-ladder/desktop lint`, `corepack pnpm --filter @prediction-ladder/desktop build`, `corepack pnpm --filter @prediction-ladder/i18n typecheck`, `corepack pnpm --filter @prediction-ladder/i18n lint`, `pnpm --filter @prediction-ladder/i18n test` with approved elevated execution (13 tests), `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, and `git diff --check` passed. The non-elevated root Turbo gates and the non-elevated i18n test hit sandbox-only package-manager/`esbuild` resolution failures before passing outside the sandbox; no product code or catalog failure was found.
- Goal 07 desktop manual-stake follow-up validation on 2026-06-05: `pnpm --filter @prediction-ladder/desktop typecheck`, `pnpm --filter @prediction-ladder/desktop test` (54 tests), `pnpm --filter @prediction-ladder/desktop lint`, `corepack pnpm --filter @prediction-ladder/desktop build`, `corepack pnpm --filter @prediction-ladder/i18n typecheck`, `corepack pnpm --filter @prediction-ladder/i18n lint`, `pnpm --filter @prediction-ladder/i18n test` with approved elevated execution (13 tests), `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, and `git diff --check` passed. The non-elevated i18n test again hit the sandbox-only `esbuild` resolution failure before passing outside the sandbox.
- Goal 07 desktop funds-diagnostic follow-up validation on 2026-06-05: `pnpm --filter @prediction-ladder/desktop typecheck`, `pnpm --filter @prediction-ladder/desktop test` (57 tests), `pnpm --filter @prediction-ladder/desktop lint`, `corepack pnpm --filter @prediction-ladder/desktop build`, `corepack pnpm --filter @prediction-ladder/i18n typecheck`, `corepack pnpm --filter @prediction-ladder/i18n lint`, `corepack pnpm --filter @prediction-ladder/i18n test` with approved elevated execution (13 tests), `pnpm typecheck`, `pnpm lint`, `corepack pnpm test` with approved elevated execution, `corepack pnpm build` with approved elevated execution, `C:\Users\1\.cargo\bin\cargo.exe test` for `apps/desktop/src-tauri` (40 passed, 1 ignored external smoke), and `git diff --check` passed. The non-elevated i18n test hit the sandbox-only `esbuild` resolution failure, and non-elevated root `test`/`build` hit the sandbox-only Turbo package-manager binary issue before passing outside the sandbox.
- Goal 07 Polymarket signer/funder correction validation on 2026-06-05: `pnpm --filter @prediction-ladder/desktop typecheck`, `pnpm --filter @prediction-ladder/desktop test` (57 tests), `pnpm --filter @prediction-ladder/desktop lint`, `corepack pnpm --filter @prediction-ladder/i18n typecheck`, `corepack pnpm --filter @prediction-ladder/i18n test` with approved elevated execution (13 tests), `corepack pnpm typecheck` with approved elevated execution, `corepack pnpm lint` with approved elevated execution, `corepack pnpm test` with approved elevated execution, `corepack pnpm build` with approved elevated execution, `C:\Users\1\.cargo\bin\cargo.exe test` for `apps/desktop/src-tauri` (41 passed, 1 ignored external smoke), and `git diff --check` passed. Non-elevated root Turbo gates again hit the sandbox-only package-manager binary issue before passing outside the sandbox; the non-elevated i18n test hit the sandbox-only `esbuild` resolution failure before passing outside the sandbox.
- Goal 07 real Polymarket account diagnostic validation on 2026-06-05: `C:\Users\1\.cargo\bin\cargo.exe test polymarket_local_profile_account_diagnostic -- --ignored --nocapture` passed with approved execution and printed only secret-free/masked diagnostics; `C:\Users\1\.cargo\bin\cargo.exe test` passed with 42 tests and 2 ignored diagnostics/smokes; `pnpm --filter @prediction-ladder/desktop typecheck` passed; `git diff --check` passed.
- Goal 07 Polymarket onboarding/cache follow-up validation on 2026-06-05: `pnpm --filter @prediction-ladder/desktop test` passed with 57 tests; `corepack pnpm --filter @prediction-ladder/i18n test` passed with approved elevated execution and 13 tests after the sandbox-only `esbuild` resolution failure; `C:\Users\1\.cargo\bin\cargo.exe test` passed with 44 Rust tests and 2 ignored diagnostics/smokes; `pnpm typecheck` and `pnpm lint` passed; `corepack pnpm test` and `corepack pnpm build` passed with approved elevated execution after the sandbox-only Turbo package-manager binary failure; `C:\Users\1\.cargo\bin\cargo.exe test polymarket_local_profile_account_diagnostic -- --ignored --nocapture` passed with approved execution and printed only masked/secret-free read-only diagnostics; `git diff --check` passed.
- Goal 07 Polymarket recommended-candidate onboarding follow-up validation on 2026-06-05: `C:\Users\1\.cargo\bin\cargo.exe test polymarket_account_candidate_recommendation` passed with 3 focused Rust tests; `pnpm --filter @prediction-ladder/desktop test` passed with 57 tests; `corepack pnpm --filter @prediction-ladder/i18n test` passed with approved elevated execution and 13 tests after a sandbox-only `pnpm` PATH failure; `C:\Users\1\.cargo\bin\cargo.exe test` passed with 52 Rust tests and 2 ignored diagnostics/smokes; `pnpm typecheck` passed after restoring the existing landing `landingContent` English alias expected by landing tests; `pnpm lint` passed; `corepack pnpm test` passed with approved elevated execution after a sandbox-only `pnpm` PATH failure; `corepack pnpm build` passed with approved elevated execution after the same sandbox-only `pnpm` PATH failure; `C:\Users\1\.cargo\bin\cargo.exe test polymarket_local_profile_account_diagnostic -- --ignored --nocapture` passed with approved elevated execution and printed only masked/secret-free read-only diagnostics; `git diff --check` passed.
- Goal 07 Polymarket Magic-export address capture validation on 2026-06-05: `C:\Users\1\.cargo\bin\cargo.exe test polymarket_` passed with 20 focused tests and 1 ignored real-account diagnostic; `C:\Users\1\.cargo\bin\cargo.exe test` passed with 55 Rust tests and 2 ignored diagnostics/smokes; `pnpm --filter @prediction-ladder/desktop test` passed with 57 tests; `corepack pnpm --filter @prediction-ladder/i18n test` passed with approved elevated execution and 13 tests after a sandbox-only `pnpm` PATH failure; `pnpm typecheck` passed; `pnpm lint` passed; non-elevated `pnpm test` and `pnpm build` hit the sandbox-only PATH issue, then `corepack pnpm test` and `corepack pnpm build` passed with approved elevated execution; `git diff --check` passed.
- Goal 07 Polymarket candidate cash diagnostic closure validation on 2026-06-05: `C:\Users\1\.cargo\bin\cargo.exe test polymarket_account_candidate` passed with 8 focused Rust tests; `pnpm --filter @prediction-ladder/desktop typecheck` passed; `corepack pnpm --filter @prediction-ladder/i18n typecheck` passed after the sandbox-only `pnpm` PATH failure; `git diff --check` passed. Broader root gates were not rerun after this small closure change to conserve Codex development budget; the immediately preceding Magic-export address capture gates had passed.
- Goal 07 Polymarket live-smoke stake/quantity blocker correction validation on 2026-06-05: `C:\Users\1\.cargo\bin\cargo.exe test live_order_request --manifest-path apps/desktop/src-tauri/Cargo.toml` passed with 6 focused tests; `pnpm --filter @prediction-ladder/desktop test -- --run appConfig tauriSecurityConfig` passed with 20 focused desktop tests; `C:\Users\1\.cargo\bin\cargo.exe test --manifest-path apps/desktop/src-tauri/Cargo.toml` passed with 61 active Rust tests and 2 ignored diagnostics/smokes after isolating the legal approval test from real local credentials; `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, and `git diff --check` passed. No live order was submitted by Codex.
- Goal 07 Polymarket live-smoke provider-rejection diagnostic validation on 2026-06-05: focused Rust tests passed for `live_order_request` (7 tests, including size below market minimum) and `polymarket_provider_error_message` (2 tests, including min-size extraction and post-only crossing). `pnpm --filter @prediction-ladder/desktop typecheck`, focused desktop tests for appConfig/liveExecutionCommands/marketDataWorkflow/orderIntentWorkflow (42 tests), `pnpm --filter @prediction-ladder/market-data test` (23 tests), `C:\Users\1\.cargo\bin\cargo.exe test --manifest-path apps/desktop/src-tauri/Cargo.toml` (64 active tests, 2 ignored diagnostics/smokes), `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, and `git diff --check` passed. No live order was submitted by Codex.
- Goal 07 Polymarket empty-provider-body diagnostic validation on 2026-06-05: `C:\Users\1\.cargo\bin\cargo.exe test polymarket_provider_error_message --manifest-path apps/desktop/src-tauri/Cargo.toml` passed with 3 focused tests including the empty HTTP status body fallback and order-context assertion; `C:\Users\1\.cargo\bin\cargo.exe test --manifest-path apps/desktop/src-tauri/Cargo.toml` passed with 65 active Rust tests and 2 ignored diagnostics/smokes; `pnpm --filter @prediction-ladder/desktop typecheck`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, and `git diff --check` passed. No live order was submitted by Codex.
- Goal 07 Polymarket official-CLOB host correction validation on 2026-06-05: official docs were rechecked and confirm CLOB trading operations, including `POST /order`, are under `https://clob.polymarket.com`; the local shell had no `POLYMARKET_CLOB_API_URL` or `POLYMARKET_CLOB_URL` override set. `C:\Users\1\.cargo\bin\cargo.exe test --manifest-path apps/desktop/src-tauri/Cargo.toml` passed with 66 active tests and 2 ignored diagnostics/smokes, including the regression that default Polymarket live/account-metrics runtime hosts use the official CLOB URL. `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, and `git diff --check` passed. No live order was submitted by Codex.
- Goal 07 open-live-order follow-up validation on 2026-06-05: after replacing auto-cancel live smoke with open live order submission plus manual cancellation, focused validation passed with `pnpm --filter @prediction-ladder/desktop typecheck`, `pnpm --filter @prediction-ladder/i18n test` (13 tests), `pnpm --filter @prediction-ladder/desktop test` (62 tests), and `C:\Users\1\.cargo\bin\cargo.exe test --manifest-path apps/desktop/src-tauri/Cargo.toml` (66 active Rust tests, 2 ignored diagnostics/smokes). Required root gates also passed: `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, and `git diff --check`. No live order was submitted by Codex.
- Goal 07 desktop UI cleanup validation on 2026-06-05: `pnpm --filter @prediction-ladder/desktop typecheck`, `pnpm --filter @prediction-ladder/desktop lint`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, and `git diff --check` passed after the Polymarket-only visible frontend cleanup. Root `pnpm test` included `@prediction-ladder/desktop` with 62 passing tests. No live order was submitted by Codex.

## Known remaining domain/spec caveats

- Rounding metadata is not yet exposed when order quantity/cost/proceeds are rounded down by decimal precision.
- Mixed known/unknown financial metric aggregation can hide unknown provider/account data unless provider adapters and live guards handle those unknowns conservatively.
- Live SELL must remain blocked unless a provider adapter proves position availability and that the order is genuinely reducing exposure.
- Tauri-owned account metrics values now have provider-owned runtime paths for Polymarket and Kalshi, but real credential/network smoke remains gated by approved local credentials, legal/local approval, and all live safety gates. The old non-committed `.local/account-metrics.local.json` bridge is dev/smoke fallback only, and normal users must not edit it to make live appear ready. Kalshi real live order placement still needs an authenticated live execution adapter before it can pass provider-adapter readiness.

## Open human validation

- Kalshi read-only official-access spike is closed for Goal 04: public docs support the markets/orderbook read path, and any real `401`/`403` response remains an explicit `provider_credentials_required` state rather than adapter success.
- Goal 04B official SDK/runtime validation is closed for the implemented read-only streaming path: Polymarket official docs list TypeScript/Python/Rust CLOB clients and direct documented public market WebSocket access; Kalshi official docs list Python/TypeScript SDKs, recommend OpenAPI/AsyncAPI as production source of truth, and require authenticated WebSocket handshakes.
- Goal 04B WebSocket-access validation is closed for implementation: Polymarket market WebSocket normalization is implemented without auth; Kalshi WebSocket normalization is implemented but real connection remains `credentials-required` until a safe Tauri-side/local credential provider supplies official handshake headers.
- Validate Kalshi commercial/provider terms before any fee, revenue-share, broker, or live execution path.
- Confirm whether USD and USDC should remain separate in global metrics or whether a later ADR should define a conversion policy.
