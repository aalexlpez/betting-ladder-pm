#!/usr/bin/env node
const target = process.argv.slice(2).join(' ') || 'script';
console.error(`NOT CONFIGURED: ${target}`);
console.error('This command must be implemented during Goal 01 — repo bootstrap. Fake TODO scripts must not pass.');
process.exit(1);
